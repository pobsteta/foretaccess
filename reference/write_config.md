# Écrit une configuration au format YAML

Écrit une configuration au format YAML

## Usage

``` r
write_config(cfg, path)
```

## Arguments

- cfg:

  Objet `foretaccess_config`.

- path:

  Chemin de sortie.

## Value

`path` de façon invisible.
