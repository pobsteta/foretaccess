# Version de la crate Rust `cablehelp` (noyau câble).

Fonction triviale du Lot 0 : elle prouve que la chaîne R \<-\> Rust
(extendr) fonctionne de bout en bout. La mécanique CableHelp arrivera au
Lot 4 (voir specs/004-cable.md).

## Usage

``` r
cablehelp_version()
```
