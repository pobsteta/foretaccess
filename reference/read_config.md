# Lit une configuration depuis un fichier YAML

Les clés absentes du YAML gardent leur défaut v3.6. Le résultat est
validé.

## Usage

``` r
read_config(path)
```

## Arguments

- path:

  Chemin d'un fichier YAML.

## Value

Un objet `foretaccess_config` validé.
