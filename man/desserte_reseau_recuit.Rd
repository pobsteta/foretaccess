% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{desserte_reseau_recuit}
\alias{desserte_reseau_recuit}
\title{Optimise a road network by simulated annealing on the insertion order (Lot 18b).}
\usage{
desserte_reseau_recuit(
  alt,
  obs,
  obs2,
  local_slope,
  zone,
  nr,
  nc,
  sources,
  network0,
  skidding,
  n_iter,
  t0,
  cooling,
  seed,
  csize,
  min_slope,
  max_slope,
  penalty_xy,
  penalty_z,
  max_diff_z,
  d_neighborhood,
  angle_hairpin,
  lmax_ab_sl,
  radius,
  prop_sl_max,
  max_slope_hairpin,
  tal,
  modhair,
  cost
)
}
\arguments{
\item{alt}{Elevation values (row-major, m).}

\item{obs}{Obstacle mask (1 = blocked), row-major.}

\item{obs2}{Excess cross-slope mask, row-major.}

\item{local_slope}{Fraction (0..1) of the neighbourhood with steep cross-slope.}

\item{zone}{Passable mask (1 = passable), row-major.}

\item{nr}{Number of raster rows.}

\item{nc}{Number of raster columns.}

\item{sources}{Parcel cells to serve, 0-based flattened, in the base order.}

\item{network0}{Existing-road cells, 0-based flattened.}

\item{skidding}{Skidding distance (m): a source within it of a road is skipped.}

\item{n_iter}{Number of annealing iterations.}

\item{t0}{Initial temperature; if \verb{<= 0}, derived from the base energy.}

\item{cooling}{Geometric cooling factor (0..1).}

\item{seed}{Seed for the reproducible neighbour moves and acceptance draws.}

\item{csize}{Cell size (m).}

\item{min_slope}{Minimum road grade (percent).}

\item{max_slope}{Maximum road grade (percent).}

\item{penalty_xy}{Turn penalty.}

\item{penalty_z}{Slope-change penalty.}

\item{max_diff_z}{Max elevation gap road/terrain (m).}

\item{d_neighborhood}{Neighbourhood radius (m).}

\item{angle_hairpin}{Hairpin angle threshold (deg).}

\item{lmax_ab_sl}{Max road length with excess cross-slope (m).}

\item{radius}{Turning radius (m).}

\item{prop_sl_max}{Max local steep-cross-slope fraction at a hairpin.}

\item{max_slope_hairpin}{Hairpin limit-angle parameter.}

\item{tal}{Hairpin limit-angle parameter.}

\item{modhair}{Hairpin spacing parameter.}

\item{cost}{Construction cost per metre (euros/m) per cell (row-major); \code{1.0} everywhere = neutral (pure geometry).}
}
\value{
A list: \code{paths} (1-based cell-index vectors of the best network),
\code{costs} (per-road costs of the best network) and \code{journal} (best-so-far cost
per iteration).
}
\description{
Energy is the total network cost; a neighbour swaps two positions in the order;
acceptance is Metropolis (\code{exp(-delta/T)}) with geometric cooling. Starts from
the base order and returns the best network met, so never worse than the plain
greedy (CA-18.1); reproducible for a fixed \code{seed} (CA-18.2). The \code{journal} is the
best-so-far cost per iteration (monotone decreasing, CA-18.4).
}
