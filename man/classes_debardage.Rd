% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skidder.R
\name{classes_debardage}
\alias{classes_debardage}
\title{Classify skidding distance into Sylvaccess distance bands}
\usage{
classes_debardage(sk, pre = NULL, config = sk$config)
}
\arguments{
\item{sk}{A \code{foretaccess_skidder} object (output of \code{\link[=skidder]{skidder()}}).}

\item{pre}{The \code{foretaccess_preprocessing} object used to run the skidder.
Optional; supplies the harvest-slope exclusion needed for the
\code{inexploitable} class.}

\item{config}{A \code{foretaccess_config}; the distance bands live in
\code{config$skidder$classes_distance_m}. Defaults to \code{sk$config}.}
}
\value{
A categorical \code{SpatRaster} (\code{classe_debardage}) with an attached
colour table, directly plottable and compatible with \code{\link[=recapituler]{recapituler()}}.
}
\description{
Turns the continuous skidding distance (\code{sk$distance_debardage}) into the
categorical \strong{"skidding distance classes"} raster that Sylvaccess exports:
distance bands (\code{config$skidder$classes_distance_m}, e.g. 0-250, 250-500,
500-1000, 1000-1500, 1500-2000, >2000 m), plus \code{inaccessible},
\code{inexploitable} (harvest slope exceeded) and \code{hors_foret}. The \code{skidder()}
engine already computes the distance; this is the display-ready product.
}
\details{
Precedence per cell: \code{hors_foret} (not forest) < distance band (reachable) <
\code{inaccessible} (forest, not reached) < \code{inexploitable} (forest, local slope
above the harvest threshold — overrides). The \code{inexploitable} class requires
\code{pre} (its \code{exclusion_mask}); without it those cells stay in their
accessibility class.
}
