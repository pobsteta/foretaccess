% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agregation.R
\name{agreger_zones}
\alias{agreger_zones}
\title{Agrégation zonale des surfaces et volumes (Lot 8)}
\usage{
agreger_zones(classes, zones, volume = NULL, id = NULL)
}
\arguments{
\item{classes}{\code{SpatRaster} catégoriel (facteur) — la sortie \code{accessibilite}
d'un moteur.}

\item{zones}{Objet \code{sf} de polygones (les entités d'agrégation).}

\item{volume}{\code{SpatRaster} de volume aligné sur \code{classes}, ou \code{NULL}.}

\item{id}{Nom de la colonne de \code{zones} identifiant chaque zone. \code{NULL}
(défaut) : un identifiant \verb{1..n} est ajouté sous la colonne \code{zone_id}.}
}
\value{
Un objet \code{sf} (classe \code{foretaccess_agregation}) : \code{zones} augmenté des
colonnes d'agrégation. Les zones sans aucune cellule ont des surfaces nulles.
}
\description{
Agrège un raster catégoriel d'accessibilité (issu d'un moteur — \code{\link[=skidder]{skidder()}},
\code{\link[=porteur]{porteur()}}, \code{\link[=camion_dfci]{camion_dfci()}} — ou d'une couverture câble) en \strong{surfaces} (ha)
et, si un raster de volume est fourni, en \strong{volumes} (m³), \strong{par zone} et par
classe. Les zones sont des polygones quelconques : massif, parcelle, commune
(US-8.2, EF-9/EF-12). C'est le pendant zonal de \code{\link[=recapituler]{recapituler()}}, qui agrège
sur l'emprise entière.
}
\details{
L'agrégation est un simple \strong{croisement raster} : les zones sont rasterisées
sur la grille du raster de classes (par leur identifiant), puis on compte les
cellules de chaque classe dans chaque zone. La surface d'une cellule vaut
\code{prod(res)/10000} ha. Une cellule non couverte par une zone est ignorée ; une
cellule de classe \code{NA} (indéterminée) compte dans la colonne \code{indetermine}.

Le résultat est un objet \code{sf} : \strong{une ligne par zone}, la géométrie d'origine
conservée, augmentée de colonnes \strong{larges} par classe — \verb{surface_<classe>_ha}
(et \verb{volume_<classe>_m3} si \code{volume}), plus \code{surface_totale_ha}. Cette forme
est directement \strong{persistable et requêtable} en base (\code{\link[=sb_write_layer]{sb_write_layer()}}).
}
\examples{
toy <- system.file("extdata/toy", package = "foretaccess")
pre <- preprocess(file.path(toy, "mnt.tif"), file.path(toy, "desserte.gpkg"),
                  file.path(toy, "foret.gpkg"))
sk <- skidder(pre)
# Deux zones : moitie ouest / moitie est de l'emprise.
e <- terra::ext(pre$mnt)
xm <- (e[1] + e[2]) / 2
za <- sf::st_as_sf(terra::as.polygons(terra::ext(e[1], xm, e[3], e[4]),
                   crs = terra::crs(pre$mnt)))
zb <- sf::st_as_sf(terra::as.polygons(terra::ext(xm, e[2], e[3], e[4]),
                   crs = terra::crs(pre$mnt)))
zones <- rbind(za, zb)
agreger_zones(sk$accessibilite, zones)
}
\seealso{
\code{\link[=recapituler]{recapituler()}}, \code{\link[=sb_write_layer]{sb_write_layer()}}
}
