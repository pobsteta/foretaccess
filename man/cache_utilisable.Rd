% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache-provenance.R
\name{cache_utilisable}
\alias{cache_utilisable}
\title{Politique de réutilisation des caches d'acquisition}
\usage{
cache_utilisable(
  chemin,
  couche,
  source = NULL,
  params = list(),
  politique = "reacquerir"
)
}
\arguments{
\item{chemin}{Chemin du fichier de cache.}

\item{couche}{Nom de la couche (\code{"desserte"}, \code{"mnt"}, ...).}

\item{source}{Identifiant de la source (typename WFS, couche WMS...).}

\item{params}{Liste nommée des paramètres qui \strong{changent le contenu}.}

\item{politique}{Une valeur de \code{\link[=politique_cache_valeurs]{politique_cache_valeurs()}}.}
}
\value{
\code{TRUE} si le cache est utilisable en l'état, \code{FALSE} sinon.
}
\description{
Que faire quand un cache existe mais \strong{n'a pas été produit avec les paramètres
de l'appel courant} ? Défaut \code{"reacquerir"} : le coût d'une ré-acquisition est
mesurable, celui d'un résultat faux ne l'est pas.
}
\details{
\itemize{
\item \code{"reacquerir"} — ignore le cache divergent et refait l'acquisition ;
\item \code{"avertir"} — sert le cache en nommant ce qui diverge ;
\item \code{"echouer"} — interrompt : pour les bancs et la CI, où un cache périmé
fausse une mesure publiée ;
\item \code{"ignorer"} — aucun contrôle, comportement antérieur à la v1.29.0.
}

Un cache \strong{sans sidecar} de provenance (antérieur à cette version) est traité
comme divergent, avec un message distinct : on ne peut pas savoir, donc on ne
suppose pas que c'est bon.
}
\seealso{
\code{specs/027}.
}
