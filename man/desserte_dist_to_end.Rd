% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{desserte_dist_to_end}
\alias{desserte_dist_to_end}
\title{Inverse cost-distance from a target cell (road-design A\* heuristic, Lot 15a).}
\usage{
desserte_dist_to_end(zone, nr, nc, csize, y_end, x_end, max_distance)
}
\arguments{
\item{zone}{Passable mask (1 = passable, 0 = blocked), row-major.}

\item{nr}{Number of raster rows.}

\item{nc}{Number of raster columns.}

\item{csize}{Cell size (m); square cells assumed.}

\item{y_end}{Target row (0-based).}

\item{x_end}{Target column (0-based).}

\item{max_distance}{Propagation ceiling (m); cells beyond it stay \code{NA}.}
}
\value{
A row-major numeric vector: 0 at the target, cumulated distance
elsewhere, \code{NA} for unreached cells.
}
\description{
Ports SylvaRoad's \code{calcul_distance_de_cout}: geometric 8-neighbour distance
(step = resolution, or \code{resolution * sqrt(2)} on the diagonal) propagated from
the target over the passable zone (\code{zone == 1}). The step cost is unit
(distance only), so the result is a lower bound of the remaining road cost —
an admissible A\* heuristic (spec 015, CA-15.8).
}
