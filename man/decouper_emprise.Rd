% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuilage.R
\name{decouper_emprise}
\alias{decouper_emprise}
\title{Découper une emprise en tuiles avec halo}
\usage{
decouper_emprise(gabarit, tuile_m, halo_m = 0)
}
\arguments{
\item{gabarit}{\code{SpatRaster} (ou chemin) dont la grille sert de référence.}

\item{tuile_m}{Côté d'une tuile, en unités du CRS. Arrondi au nombre entier de
cellules supérieur.}

\item{halo_m}{Largeur du halo, en unités du CRS. Défaut \code{0}.}
}
\value{
Un objet de classe \code{foretaccess_tuiles} :
\describe{
\item{\code{tuiles}}{\code{data.frame}, une ligne par tuile : \code{id}, les lignes/colonnes
de la fenêtre d'écriture (\code{l1}, \code{l2}, \code{c1}, \code{c2}), celles de la fenêtre de
calcul (\code{hl1}, \code{hl2}, \code{hc1}, \code{hc2}), et les côtés ouverts
(\code{ouvert_haut}, \code{ouvert_bas}, \code{ouvert_gauche}, \code{ouvert_droite}).}
\item{\code{nrow}, \code{ncol}, \code{res}}{la grille du gabarit.}
\item{\code{tuile_cel}, \code{halo_cel}}{tuile et halo, en cellules.}
}
}
\description{
Découpe la grille d'un raster gabarit en \strong{fenêtres d'écriture disjointes} (les
tuiles), chacune assortie d'une \strong{fenêtre de calcul} élargie d'un halo. Le halo
n'est pas un chevauchement à fusionner : il ne sert qu'au calcul, et n'est jamais
écrit. La recomposition est donc une mosaïque, sans règle de fusion (spec 007 §4.5).
}
\details{
Le découpage travaille en \strong{indices de lignes et de colonnes}, jamais en
coordonnées : deux tuiles adjacentes partagent une frontière exacte, sans risque
d'arrondi sur les emprises.

Chaque tuile porte les \strong{côtés ouverts} de sa fenêtre de calcul : ceux par lesquels
un chemin venu du reste du territoire peut entrer. Un côté qui coïncide avec le bord
de l'emprise est \strong{fermé} — rien n'existe au-delà. Cette distinction évite au
certificat (spec 007 §4.3) d'être inutilement pessimiste sur les tuiles de bordure.
}
\examples{
r <- terra::rast(nrows = 10, ncols = 10, xmin = 0, xmax = 100,
                 ymin = 0, ymax = 100, crs = "EPSG:2154")
decouper_emprise(r, tuile_m = 50, halo_m = 10)
}
\seealso{
\code{\link[=fenetre_tuile]{fenetre_tuile()}}, \code{\link[=certifier_propagation]{certifier_propagation()}}
}
