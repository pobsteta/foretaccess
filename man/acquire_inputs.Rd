% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acquire.R
\name{acquire_inputs}
\alias{acquire_inputs}
\title{Acquiert les entrées ForêtAccess depuis une AOI}
\usage{
acquire_inputs(
  aoi,
  sources = c("mnt", "desserte", "foret", "obstacles", "cadastre"),
  cache_dir = tempdir(),
  res_m = 5,
  crs = 2154,
  buffer_m = 100,
  overwrite = FALSE,
  country = "FR"
)
}
\arguments{
\item{aoi}{Emprise : chemin d'un fichier vectoriel, ou objet \code{sf}/\code{sfc}
polygonal. \strong{Doit porter un CRS} (règle stricte du projet).}

\item{sources}{Sources à acquérir (sous-ensemble de \code{mnt}, \code{desserte}, \code{foret},
\code{obstacles}, \code{cadastre}).}

\item{cache_dir}{Répertoire de cache. Défaut \code{tempdir()}.}

\item{res_m}{Résolution du MNT (m). Défaut 5.}

\item{crs}{Code EPSG de sortie. Défaut 2154.}

\item{buffer_m}{Buffer d'emprise (m) autour de l'AOI. Défaut 100.}

\item{overwrite}{Re-télécharger même si le cache existe. Défaut \code{FALSE}.}

\item{country}{Code pays ISO. Défaut \code{"FR"}.}
}
\value{
Un objet \code{foretaccess_inputs} : \code{mnt} (chemin raster), \code{desserte},
\code{foret}, \code{obstacles}, \code{parcellaire} (\code{sf} ou \code{NULL}), \code{aoi} (\code{sf} stricte),
\code{meta} (sources, CRS, buffer, date), \code{cache_dir}. Directement consommable par
\code{\link[=preprocess]{preprocess()}}.
}
\description{
Télécharge automatiquement, à partir d'un simple polygone d'emprise (AOI), les
couches attendues par \code{\link[=preprocess]{preprocess()}} : MNT (RGE ALTI), desserte (BD TOPO),
forêt (BD Forêt v2), obstacles (OpenStreetMap) et, en option, le parcellaire
cadastral. Approche \strong{config-driven} (patron nemeton) : endpoints et couches
sont déclarés dans \verb{inst/datasources/<pays>.json}, jamais codés en dur
(voir \link{datasources}).
}
\details{
Les clients réseau (\code{happign} pour l'IGN, \code{osmdata} pour OSM) sont en
\strong{Suggests} : le cœur du paquet s'installe sans eux, et seule l'acquisition
les requiert (message d'installation ciblé sinon). Chaque source est mise en
\strong{cache} sous \verb{cache_dir/layers/<couche>/} et réutilisée au 2ᵉ appel, sauf
\code{overwrite = TRUE}.

L'AOI est reprojetée dans \code{crs} (EPSG:2154 par défaut). Un \strong{buffer}
(\code{buffer_m}, défaut 100 m) élargit l'emprise d'acquisition pour capter la
desserte juste hors de l'AOI — utile au plus court chemin (Lot 2) ; les couches
sont découpées sur cette emprise élargie. L'AOI stricte est conservée dans le
résultat (\verb{$aoi}).
}
\examples{
\dontrun{
aoi <- sf::st_read("mon_massif.gpkg")
inputs <- acquire_inputs(aoi, cache_dir = "cache")
pre <- preprocess(inputs$mnt, inputs$desserte, inputs$foret,
                  obstacles_complets = inputs$obstacles)
}
}
\seealso{
\code{\link[=preprocess]{preprocess()}}, \link{datasources}, \code{\link[=acquire_mnt]{acquire_mnt()}}
}
