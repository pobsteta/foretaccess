% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acquire.R
\name{acquire_inputs}
\alias{acquire_inputs}
\title{Acquiert les entrées ForêtAccess depuis une AOI}
\usage{
acquire_inputs(
  aoi,
  sources = c("mnt", "desserte", "foret", "obstacles", "cadastre"),
  cache_dir = tempdir(),
  res_m = 5,
  res_lidar_m = 1,
  crs = 2154,
  buffer_m = 100,
  overwrite = FALSE,
  country = "FR",
  dfci = TRUE,
  volume = NULL,
  champ_volume = "P1",
  config = NULL
)
}
\arguments{
\item{aoi}{Emprise : chemin d'un fichier vectoriel, ou objet \code{sf}/\code{sfc}
polygonal. \strong{Doit porter un CRS} (règle stricte du projet).}

\item{sources}{Sources à acquérir (sous-ensemble de \code{mnt}, \code{desserte}, \code{foret},
\code{obstacles}, \code{cadastre}).}

\item{cache_dir}{Répertoire de cache. Défaut \code{tempdir()}.}

\item{res_m}{Résolution du MNT (m). Défaut 5.}

\item{res_lidar_m}{Résolution fine (m) de téléchargement du MNT LIDAR HD,
agrégée à \code{res_m} (cf. \code{\link[=acquire_mnt]{acquire_mnt()}}). Défaut 1.}

\item{crs}{Code EPSG de sortie. Défaut 2154.}

\item{buffer_m}{Buffer d'emprise (m) autour de l'AOI. Défaut 100.}

\item{overwrite}{Re-télécharger même si le cache existe. Défaut \code{FALSE}.}

\item{country}{Code pays ISO. Défaut \code{"FR"}.}

\item{dfci}{Alimenter le flag DFCI (\code{CL_DFCI}) sur la desserte, source du camion
DFCI (spec 006) : réseau OSM \code{ref:FR:DFCI} (\code{\link[=acquire_dfci]{acquire_dfci()}}), avec repli
géométrique (\code{\link[=flag_dfci]{flag_dfci()}}) si OSM ne rend rien. Défaut \code{TRUE}.}

\item{volume}{Volume sur pied à injecter (facultatif). \strong{Jamais téléchargé} :
sa source est un inventaire ou l'indicateur \strong{P1} de Nemeton (cf.
\code{\link[=volume_depuis_p1]{volume_depuis_p1()}}). Accepte un \code{SpatRaster} en m3/ha, un chemin de raster,
ou un \code{sf} d'unités portant un champ m3/ha (rasterisé via \code{\link[=volume_depuis_p1]{volume_depuis_p1()}}).
Aligné sur la grille du \strong{MNT bufferisé} : le câble somme le volume jusque
dans le halo, un volume tronqué à l'AOI sous-estime l'IPC des lignes de bord
(spec 019). CRS différent du MNT : erreur (ADR-004) ; grille différente (même
CRS) : rééchantillonné, avec avertissement. \code{NULL} (défaut) : pas de volume.}

\item{champ_volume}{Nom du champ m3/ha, utilisé seulement quand \code{volume} est un
\code{sf}. Défaut \code{"P1"} (la colonne écrite par Nemeton).}

\item{config}{Objet \code{\link[=foretaccess_config]{foretaccess_config()}} fournissant les seuils DFCI
(\code{dfci$tol_appariement_m}, \code{emprise_min_m}, \code{rayon_retournement_m}) passés à
\code{\link[=flag_dfci]{flag_dfci()}}. Défaut \code{NULL} (seuils par défaut).}
}
\value{
Un objet \code{foretaccess_inputs} : \code{mnt} (chemin raster), \code{desserte},
\code{foret}, \code{obstacles}, \code{parcellaire} (\code{sf} ou \code{NULL}), \code{volume} (\code{SpatRaster}
m3/ha ou \code{NULL}), \code{aoi} (\code{sf} stricte), \code{meta} (sources, CRS, buffer, date),
\code{cache_dir}. Directement consommable par \code{\link[=preprocess]{preprocess()}}.
}
\description{
Télécharge automatiquement, à partir d'un simple polygone d'emprise (AOI), les
couches attendues par \code{\link[=preprocess]{preprocess()}} : MNT (RGE ALTI), desserte (BD TOPO),
forêt (BD Forêt v2), obstacles (OpenStreetMap) et, en option, le parcellaire
cadastral. Approche \strong{config-driven} (patron nemeton) : endpoints et couches
sont déclarés dans \verb{inst/datasources/<pays>.json}, jamais codés en dur
(voir \link{datasources}).
}
\details{
Les clients réseau (\code{happign} pour l'IGN, \code{osmdata} pour OSM) sont en
\strong{Suggests} : le cœur du paquet s'installe sans eux, et seule l'acquisition
les requiert (message d'installation ciblé sinon). Chaque source est mise en
\strong{cache} sous \verb{cache_dir/layers/<couche>/} et réutilisée au 2ᵉ appel, sauf
\code{overwrite = TRUE}.

L'AOI est reprojetée dans \code{crs} (EPSG:2154 par défaut). Un \strong{buffer}
(\code{buffer_m}, défaut 100 m) élargit l'emprise d'acquisition pour capter la
desserte juste hors de l'AOI — utile au plus court chemin (Lot 2) ; les couches
sont découpées sur cette emprise élargie. L'AOI stricte est conservée dans le
résultat (\verb{$aoi}).
}
\examples{
\dontrun{
aoi <- sf::st_read("mon_massif.gpkg")
inputs <- acquire_inputs(aoi, cache_dir = "cache")
pre <- preprocess(inputs$mnt, inputs$desserte, inputs$foret,
                  obstacles_complets = inputs$obstacles)

# Volume via l'indicateur P1 de Nemeton (inventaire ou MNH LiDAR). Nemeton
# calcule P1 sur l'emprise bufferisee ; acquire_inputs le rasterise.
p1 <- nemeton::indicateur_p1_volume(parcelles, chm = mnh_lidar)
inputs <- acquire_inputs(aoi, cache_dir = "cache", volume = p1)
pre <- preprocess(inputs$mnt, inputs$desserte, inputs$foret,
                  volume = inputs$volume)
}
}
\seealso{
\code{\link[=preprocess]{preprocess()}}, \code{\link[=volume_depuis_p1]{volume_depuis_p1()}}, \link{datasources}, \code{\link[=acquire_mnt]{acquire_mnt()}}
}
