% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_optim.R
\name{optimiser_reseau}
\alias{optimiser_reseau}
\title{Optimise a forest-road network over insertion orders}
\usage{
optimiser_reseau(
  pre,
  cout,
  parcelles,
  desserte_existante,
  strategie = c("multistart", "recuit", "riprute"),
  heuristique = c("plus_proche", "plus_gros_volume", "aleatoire"),
  n_start = 16,
  n_iter = 200,
  temp0 = 0,
  refroidissement = 0.95,
  max_passes = 6,
  graine = 1,
  skidding_m = 0,
  volume_champ = NULL,
  pondere_cout = FALSE,
  config = foretaccess_config()
)
}
\arguments{
\item{pre}{A \code{foretaccess_preprocessing} object (DEM, terrain slope).}

\item{cout}{A \code{foretaccess_cout_construction} object (Lot 14). Its euros/m
surface is read \strong{only if \code{pondere_cout = TRUE}}; at the \code{FALSE} default
only the \code{franchissable} mask is used. See \code{pondere_cout}.}

\item{parcelles}{An \code{sf} POLYGON of the areas to serve.}

\item{desserte_existante}{An \code{sf} LINESTRING of the network to connect to.}

\item{strategie}{Optimisation strategy: \code{"multistart"} (default), \code{"recuit"}
(simulated annealing on the insertion order) or \code{"riprute"} (rip-up & reroute
local search).}

\item{heuristique}{Base ordering of parcels (trial 0), see \code{\link[=reseau_desserte]{reseau_desserte()}}.}

\item{n_start}{Number of insertion orders to try (\code{"multistart"}).}

\item{n_iter}{Number of annealing iterations (\code{"recuit"}).}

\item{temp0}{Initial annealing temperature (\code{"recuit"}); \verb{<= 0} derives it from
the base network cost.}

\item{refroidissement}{Geometric cooling factor in \verb{(0, 1)} (\code{"recuit"}).}

\item{max_passes}{Maximum improvement passes (\code{"riprute"}).}

\item{graine}{Integer seed for the reproducible order permutations / moves.}

\item{skidding_m}{Skidding distance (m): a parcel cell within it of a road is
served without building a road.}

\item{volume_champ}{Optional name of the parcel volume column (for the
\code{"plus_gros_volume"} base ordering).}

\item{pondere_cout}{If \code{TRUE}, weights the trace by the Lot 14 construction
cost surface (\code{cout$cout}, euros/m) instead of geometric distance.}

\item{config}{A \code{foretaccess_config}; the solver settings live in
\code{config$desserte$trace}.}
}
\value{
A \code{foretaccess_reseau} object (same as Lot 16) for the best network
found, enriched with \code{strategie} and \code{journal} (total cost per trial).
}
\description{
Wraps the Lot 16 greedy builder in an optimisation layer that explores the
space of parcel insertion orders and keeps the cheapest network. The greedy
order is always trial 0, so the result is never worse than the plain greedy of
\code{\link[=reseau_desserte]{reseau_desserte()}} (CA-18.1). Currently the \code{"multistart"} strategy is
available (parallel over K perturbed orders, \code{rayon}); \code{"recuit"} (simulated
annealing) and \code{"riprute"} (rip-up & reroute) are planned (Lot 18b/18c).
}
\section{Performance}{

Each trial is a full greedy build reusing a \strong{single} neighbourhood table and
running the trials in \strong{parallel} (\code{rayon}). Since the greedy trace was made
corridor-bounded (see \code{\link[=reseau_desserte]{reseau_desserte()}} \emph{Performance}), the optimisers are now
tractable at interactive scale: on a departmental run, \code{n_start = 16} costs about
the same wall-clock as one greedy build. Reasonable exposable defaults:
\code{n_start} 8-32, \code{n_iter} 100-300 -- no hard cap is needed below those. The
dominant lever remains \strong{\code{skidding_m}} (set it to the real skidding distance;
see \code{\link[=reseau_desserte]{reseau_desserte()}}), which governs the number of traces per build.
}

\section{Surface publique}{

\strong{\code{optimiser_reseau()} est la seule facade supportee.} Les bindings
\code{desserte_reseau_multistart()}, \code{desserte_reseau_recuit()} et
\code{desserte_reseau_riprute()} sont exportes parce qu'extendr exporte ce qu'il
compile, pas parce qu'ils constituent une API : ils prennent des vecteurs
aplatis (\code{alt}, \code{obs}, \code{nr}, \code{nc}, indices 0-based) dont la construction et la
coherence sont precisement ce que cette fonction garantit. Les appeler
directement, c'est reimplementer \code{.reseau_preparer()} -- et se tromper d'un
cran d'indice sans que rien ne le signale.

Meme statut pour \code{desserte_dist_to_end()} : primitive du solveur (distance
geodesique a la cible dans la zone franchissable, heuristique de l'A\*), sans
usage propre hors de ce contexte.
}

