% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte-detectee.R
\name{specs_depuis_calibration}
\alias{specs_depuis_calibration}
\title{Turn a dessertR calibration into detection specs}
\usage{
specs_depuis_calibration(
  calibration,
  surface = specs_desserte_calibrees()$surface,
  c_vessel = specs_desserte_calibrees()$c_vessel
)
}
\arguments{
\item{calibration}{Either the full \code{dsr_calibrer_specs()} result or its
\verb{$specs} element directly. Both are accepted.}

\item{surface}{Bounds for the point-cloud channels. Defaults to the frozen
ones; \code{NULL} leaves them to dessertR.}

\item{c_vessel}{Frangi anchor. Defaults to the frozen one; \code{NULL} makes the
vesselness extent-relative.}
}
\value{
A list shaped like \code{\link[=specs_desserte_calibrees]{specs_desserte_calibrees()}}, usable as the \code{specs}
argument of \code{\link[=detecter_desserte]{detecter_desserte()}}.
}
\description{
\code{dessertR::dsr_calibrer_specs()} returns bounds calibrated \strong{on your own
data}, and advises using them when the frozen ones saturate. Its \verb{$specs} is
a \strong{flat} list of channels; \code{\link[=detecter_desserte]{detecter_desserte()}} expects the \strong{nested}
shape of \code{\link[=specs_desserte_calibrees]{specs_desserte_calibrees()}} (\code{geomorpho} / \code{surface} / \code{c_vessel}).
Two contracts for one word -- this bridges them.
}
\details{
The flat calibration \emph{is} the \code{geomorpho} group: \verb{$specs} is documented as
"directement utilisable" by \code{dsr_conductivite()}, which is exactly what
\code{\link[=detecter_desserte]{detecter_desserte()}} feeds with \code{specs$geomorpho}.

\strong{What a calibration cannot give you}, and why the other two groups keep
their frozen defaults:
\itemize{
\item \code{surface} -- those channels (\code{taux_penetration}, \code{densite_sol},
\code{h_couvert}) come from the \strong{point cloud}, not from the DEM stack that was
calibrated. \code{dsr_calibrer_specs()} never sees them.
\item \code{c_vessel} -- the Frangi anchor, produced by \code{dessertR::dsr_c_vessel()}, not
by the calibration. Without it the vesselness channel is rescaled on
whatever extent you pass, and \code{seuil} stops being comparable between sites.
}

Mixing locally calibrated \code{geomorpho} bounds with frozen \code{surface} bounds is a
\strong{deliberate compromise}, not an oversight: it is still better than frozen
bounds that saturate on your massif. Pass \code{surface = NULL} to opt out and let
dessertR derive them, at the cost of extent-relative results.
}
\examples{
# Forme attendue, sans appeler dessertR :
plat <- list(rugosite = list(type = "croissante", a = 0.04, b = 0.17, poids = 2))
str(specs_depuis_calibration(plat)$geomorpho)
}
\seealso{
\code{\link[=detecter_desserte]{detecter_desserte()}}, \code{\link[=specs_desserte_calibrees]{specs_desserte_calibrees()}},
\code{dessertR::dsr_calibrer_specs()}, \code{specs/026}.
}
