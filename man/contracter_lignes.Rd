% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_flux.R
\name{contracter_lignes}
\alias{contracter_lignes}
\title{Contract a designed network's grid polylines into clean tronçons}
\usage{
contracter_lignes(reseau)
}
\arguments{
\item{reseau}{A \code{foretaccess_reseau} object (Lot 16).}
}
\value{
An \code{sf} LINESTRING, one feature per contracted tronçon, with \code{id},
\code{de} / \code{vers} (node cell ids) and \code{longueur} (m). Empty roads abort.
}
\description{
\code{\link[=reseau_desserte]{reseau_desserte()}}'s \verb{$lignes} follow the raster grid step by step (thousands
of tiny segments), which is heavy and staircase-like to display. This applies
the \strong{same topological contraction} as \code{\link[=vectoriser_reseau]{vectoriser_reseau()}} -- degree-2
chains merged into \code{troncons} between junctions (degree >= 3), leaves and
outlets -- returning a \strong{tidy vector} of the created network, without building
the full flux graph. The result is ready to feed \code{\link[=vectoriser_reseau]{vectoriser_reseau()}} /
\code{\link[=typer_desserte]{typer_desserte()}} or to display directly instead of the \verb{$reseau} raster.
}
\examples{
\dontrun{
res <- reseau_desserte(pre, cout, parcelles, desserte_existante)
lignes_propres <- contracter_lignes(res) # affichage vecteur net
}
}
\seealso{
\code{\link[=reseau_desserte]{reseau_desserte()}} (produces the fine \verb{$lignes}),
\code{\link[=vectoriser_reseau]{vectoriser_reseau()}} (the typing entry point that contracts likewise).
}
