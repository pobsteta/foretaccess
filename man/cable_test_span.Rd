% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cable_test_span}
\alias{cable_test_span}
\title{Teste un segment de câble entre les points \code{pg} et \code{posi} du profil, portant
des supports de hauteurs \code{hg} et \code{hd} : pré-filtre, pente dans
\verb{[slope_min, slope_max]}, contrainte d'angle au support intermédiaire
(\code{angle_intsup}) vis-à-vis du segment précédent (\code{slope_prev}, \code{-9999} si
aucun), puis \code{find_lomin}. Renvoie un vecteur
\code{c(faisable, D, H, diag, slope, fact, Xup, Zup, Lo, Th, Tv, Tcalc, F)}.}
\usage{
cable_test_span(
  line_x,
  line_z,
  pg,
  posi,
  hg,
  hd,
  hline_min,
  hline_max,
  slope_min,
  slope_max,
  alts,
  f_o,
  tmax,
  q1,
  q2,
  q3,
  eao,
  csize,
  angle_intsup,
  dsupdep,
  slope_prev
)
}
\arguments{
\item{line_x}{Horizontal positions along the terrain profile (m).}

\item{line_z}{Terrain altitudes along the profile (m).}

\item{pg}{Index of the near support in the profile.}

\item{posi}{Index of the far support in the profile.}

\item{hg}{Height of the near support (m).}

\item{hd}{Height of the far support (m).}

\item{hline_min}{Minimum ground clearance of the cable (m).}

\item{hline_max}{Maximum height of the cable (m).}

\item{slope_min}{Minimum admissible slope of the span (rad).}

\item{slope_max}{Maximum admissible slope of the span (rad).}

\item{alts}{Terrain altitudes under the line, sampled every 0.5 m (m).}

\item{f_o}{Gravity force of the load and carriage (N).}

\item{tmax}{Admissible cable tension (N).}

\item{q1}{Linear mass of the skyline (kg/m).}

\item{q2}{Linear mass of the mainline / traction cable (kg/m).}

\item{q3}{Linear mass of the return cable (kg/m).}

\item{eao}{Young's modulus times the cable section (N).}

\item{csize}{Sweep step of the load position (m).}

\item{angle_intsup}{Maximum slope change allowed at an intermediate support (rad).}

\item{dsupdep}{Extra cable length on the departure side (m).}

\item{slope_prev}{Slope of the previous span (rad), or -9999 if none.}
}
\value{
A length-13 numeric vector (see the description for the fields).
}
\description{
Teste un segment de câble entre les points \code{pg} et \code{posi} du profil, portant
des supports de hauteurs \code{hg} et \code{hd} : pré-filtre, pente dans
\verb{[slope_min, slope_max]}, contrainte d'angle au support intermédiaire
(\code{angle_intsup}) vis-à-vis du segment précédent (\code{slope_prev}, \code{-9999} si
aucun), puis \code{find_lomin}. Renvoie un vecteur
\code{c(faisable, D, H, diag, slope, fact, Xup, Zup, Lo, Th, Tv, Tcalc, F)}.
}
