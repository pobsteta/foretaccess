% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/porteur.R
\name{porteur}
\alias{porteur}
\title{Moteur d'accessibilité porteur (forwarder)}
\usage{
porteur(pre, config = foretaccess_config(), write_dir = NULL, bord = NULL)
}
\arguments{
\item{pre}{Objet \code{foretaccess_preprocessing} issu de \code{\link[=preprocess]{preprocess()}}.}

\item{config}{Objet \code{\link[=foretaccess_config]{foretaccess_config()}}.}

\item{write_dir}{Répertoire d'écriture des rasters, ou \code{NULL}.}

\item{bord}{Côtés ouverts de la fenêtre quand \code{pre} est une \strong{tuile} (voir
\code{\link[=certifier_propagation]{certifier_propagation()}}). \code{NULL} (défaut) : \code{pre} couvre tout le territoire.}
}
\value{
Un objet de classe \code{foretaccess_porteur}, de structure parallèle à
\code{\link[=skidder]{skidder()}} mais sans \code{distance_treuillage} : \code{accessibilite},
\code{distance_conduite}, \code{distance_grappin}, \code{distance_trainage_piste},
\code{distance_debardage}, \code{allocation}, \code{certifie}, \code{recap}, \code{grid}, \code{config}.
}
\description{
Applique les règles Sylvaccess v3.6 au jeu de rasters produit par \code{\link[=preprocess]{preprocess()}}.
Le porteur diffère profondément du skidder : sa conduite est un \strong{balayage radial}
depuis le réseau (\code{\link[=conduire]{conduire()}}), non un plus court chemin, et il n'a \strong{pas de treuil}
mais un \strong{grappin} de portée fixe. Voir \code{specs/003-porteur.md}.
}
\details{
Trois mécanismes, dans l'ordre de priorité :
\itemize{
\item la \strong{conduite} (\code{\link[=conduire]{conduire()}}) : l'engin roule, sous ses contraintes de pente en
long, de dévers et de distance en pente forte. Ces cellules sont \code{parcourable} ;
\item le \strong{grappin} : depuis le contour de la zone conduite, une extension de
\code{portee_grue_m} (défaut 8 m) sur le terrain récoltable. Ces cellules sont
\code{accessible} sans être \code{parcourable} ;
\item la \strong{distance sur piste}, mutualisée avec le skidder (Lot 2).
}
}
\examples{
toy <- system.file("extdata/toy", package = "foretaccess")
pre <- preprocess(file.path(toy, "mnt.tif"), file.path(toy, "desserte.gpkg"),
                  file.path(toy, "foret.gpkg"))
po <- porteur(pre)
po$recap
}
\seealso{
\code{\link[=skidder]{skidder()}}, \code{\link[=conduire]{conduire()}}, \code{\link[=traiter_par_tuiles]{traiter_par_tuiles()}}
}
