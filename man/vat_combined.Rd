% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vat_combined.R
\name{vat_combined}
\alias{vat_combined}
\title{CVAT composite (Combined VAT) from a DEM -- RVT QGIS default}
\usage{
vat_combined(mnt, params = cvat_terrain_params(), as_byte = FALSE)
}
\arguments{
\item{mnt}{Digital terrain model as a single-layer \code{SpatRaster} (square cells;
the first layer is used if several).}

\item{params}{Terrain parameter sets, default \code{\link[=cvat_terrain_params]{cvat_terrain_params()}}.}

\item{as_byte}{If \code{TRUE}, return the 8-bit raster (\verb{0..255}, \code{NA}->255) as the
plugin writes it (\verb{*_CVAT_8bit.tif}); if \code{FALSE} (default), the float
composite in \verb{[0, 1]}.}
}
\value{
A single-layer \code{SpatRaster} named \code{cvat}, aligned to \code{mnt}.
}
\description{
Reproduces the RVT QGIS plugin's default archaeological product, \strong{CVAT}
(Combined Visualization for Archaeological Topography): the 50/50 blend of two
\code{\link[=vat_archeo]{vat_archeo()}}-style VATs computed with the \code{general} and \code{flat} terrain
presets. It adapts to both flat and steep ground far better than a single VAT.
}
\details{
The SVF and positive-openness channels come from the validated Rust kernel
\code{\link[=rvt_svf_opns]{rvt_svf_opns()}} (search radii taken \strong{in pixels} from the terrain preset);
slope, hillshade and the 8-bit conversion are ported to the letter from
\code{rvt/vis.py} (\code{slope_aspect}, \code{hillshade}, \code{byte_scale}) -- RVT uses 2-cell
central differences with edge padding, \strong{not} \code{terra}'s Horn slope. Feed a
DEM at \strong{1 m or finer} (the 0.5 m IGN LiDAR HD DTM). Validated pixel-to-pixel
against the RVT oracle (\code{data-raw/oracle_rvt.R}).
}
\examples{
mnt <- terra::rast(nrows = 30, ncols = 30, xmin = 0, xmax = 30,
                   ymin = 0, ymax = 30, crs = "EPSG:2154")
terra::values(mnt) <- 100 + as.vector(terra::rowFromCell(mnt, 1:900)) * 0.2
cvat <- vat_combined(mnt)
names(cvat)
}
\seealso{
\code{\link[=vat_archeo]{vat_archeo()}} (single VAT), \code{\link[=cvat_terrain_params]{cvat_terrain_params()}}, \code{\link[=blend_rvt]{blend_rvt()}}.
}
