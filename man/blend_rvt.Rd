% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vat_archeo.R
\name{blend_rvt}
\alias{blend_rvt}
\title{Layered RVT blend of a channel stack into a single composite}
\usage{
blend_rvt(stack, layers)
}
\arguments{
\item{stack}{A \code{SpatRaster} whose layers are the channels to blend, in the
\strong{same order} as \code{layers} (layer 1 = top, last layer = bottom / base).}

\item{layers}{A list of per-layer specs (see \code{\link[=vat_default_layers]{vat_default_layers()}}); each is
a list with \code{min}, \code{max}, \code{invert}, \code{mode} (\code{"normal"}, \code{"multiply"},
\code{"screen"}, \code{"overlay"}, \code{"soft_light"}, \code{"luminosity"} -- RVT's mode set)
and \code{opacity} (0..1). Must have one entry per layer of \code{stack}.}
}
\value{
A single-layer \code{SpatRaster} named \code{vat}, values in \verb{[0, 1]}, aligned to
\code{stack}; \code{NA} where any contributing layer is \code{NA}.
}
\description{
Folds a multi-layer \code{SpatRaster} -- ordered \strong{top to bottom} -- into one
grayscale composite in \verb{[0, 1]}, following the RVT blending model: each layer
is linearly normalized (with optional inversion), fused with the accumulated
background through its blend mode, then alpha-composited by its opacity. The
bottom layer is the base canvas. Thin GIS wrapper: the arithmetic runs on the
extracted cell values (mirroring \code{\link[=micro_relief]{micro_relief()}}).
}
\details{
Reproduces RVT's blend faithfully, \strong{including} the quirk that \code{"overlay"}
and \code{"soft_light"} mutate the background in place, which \strong{neutralizes their
opacity} (the layer applies at 100\%). This is intentional -- it keeps the
output identical to RVT's own VAT -- and is pinned to the RVT_py oracle
(\code{data-raw/oracle_rvt.R}); do not "fix" it.
}
\seealso{
\code{\link[=vat_archeo]{vat_archeo()}}, \code{\link[=vat_default_layers]{vat_default_layers()}}.
}
