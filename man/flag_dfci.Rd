% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dfci-source.R
\name{flag_dfci}
\alias{flag_dfci}
\title{Pose le flag DFCI (CL_DFCI) sur la desserte}
\usage{
flag_dfci(
  desserte,
  dfci_lignes = NULL,
  retournements = NULL,
  emprise_min_m = 10,
  rayon_retournement_m = 20,
  tol_appariement_m = 10
)
}
\arguments{
\item{desserte}{An \code{sf} of line features (from \code{\link[=acquire_desserte]{acquire_desserte()}}).}

\item{dfci_lignes}{An \code{sf} of DFCI tracks (from \code{\link[=acquire_dfci]{acquire_dfci()}}), or \code{NULL}.}

\item{retournements}{An \code{sf} of turning-area points, or \code{NULL}.}

\item{emprise_min_m}{Minimum width of a through DFCI track (m). Default 10.}

\item{rayon_retournement_m}{Max distance dead-end tip <-> turning area (m).
Default 20.}

\item{tol_appariement_m}{Matching tolerance desserte <-> OSM DFCI (m). Default 10.}
}
\value{
\code{desserte} with an added integer column \code{dfci} (0/1).
}
\description{
Marks the road segments that make up the fire-defence network (DFCI), the
source of the DFCI truck engine (spec 006). The flag is orthogonal to the
\code{route}/\code{piste}/\code{reseau_public} classes. Two strategies, in order:
\itemize{
\item \strong{Route A (dedicated OSM source)} -- a segment is flagged when it coincides
(within \code{tol_appariement_m}) with an OSM DFCI track (\code{ref:FR:DFCI}).
\item \strong{Route B (geometric fallback)} -- when \code{dfci_lignes} is empty/\code{NULL}, keep
the tracks that are either \emph{through-routes} wider than \code{emprise_min_m}, or
\emph{dead-ends fitted with a turning area} (within \code{rayon_retournement_m} of the
dangling end). This is an explicitly heuristic guess (false positives
possible), logged as such.
}
}
\seealso{
\code{\link[=acquire_dfci]{acquire_dfci()}}, \code{\link[=acquire_desserte]{acquire_desserte()}}
}
