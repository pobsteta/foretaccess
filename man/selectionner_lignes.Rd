% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection.R
\name{selectionner_lignes}
\alias{selectionner_lignes}
\title{Selection multicritere des lignes cable (Lot 5)}
\usage{
selectionner_lignes(cable, config = cable$config)
}
\arguments{
\item{cable}{Objet \code{foretaccess_cable} (voir \code{\link[=potentiel_cable]{potentiel_cable()}}), dont la
table \verb{$lignes} porte les candidates.}

\item{config}{Objet \code{foretaccess_config} ; les poids/limites vivent dans
\code{config$cable$selection}. Par defaut, celui du \code{cable}.}
}
\value{
Un objet \code{foretaccess_selection} : \code{lignes} (objet \code{sf} LINESTRING des
lignes retenues, avec attributs), \code{couverture} (\code{SpatRaster} de la zone
couverte), \code{config}.
}
\description{
Reproduit \code{select_best_lines} / \code{create_best_table} de Sylvaccess v3.6 : parmi
les lignes candidates du balayage cable (\code{\link[=potentiel_cable]{potentiel_cable()}}), en selectionne
un sous-ensemble non redondant maximisant la couverture selon des criteres
ponderes. Enchaine : filtrage par limites, score pondere normalise,
classement, selection \strong{gloutonne} (une ligne n'est retenue que si elle
apporte assez de surface \strong{nouvelle}).
}
\details{
Six criteres (EF-7) : surface (max), supports (min), sens (amont/aval),
longueur (min/max), volume (max), IPC (max). Chacun a un \strong{poids} (0 = ignore)
et une \strong{limite}, dans \code{config$cable$selection}. Sans donnee de volume, les
criteres volume/IPC sont neutralises.
}
