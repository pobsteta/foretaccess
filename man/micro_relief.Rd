% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/micro_relief.R
\name{micro_relief}
\alias{micro_relief}
\title{Micro-relief channels from a DEM (RVT sky-view factor & openness, spec 021)}
\usage{
micro_relief(
  mnt,
  radius_m = 10,
  radius_min_m = NULL,
  num_directions = 16L,
  canaux = c("svf", "openness_pos", "openness_neg")
)
}
\arguments{
\item{mnt}{Digital terrain model as a single-layer \code{SpatRaster} (assumed
square cells; the first layer is used if several).}

\item{radius_m}{Maximal horizon search radius in \strong{metres}. Default 10.}

\item{radius_min_m}{Minimal search radius in metres (noise reduction). Default
\code{NULL} -> one pixel.}

\item{num_directions}{Number of azimuth directions swept. Default 16.}

\item{canaux}{Which channels to return, any of \code{"svf"}, \code{"openness_pos"},
\code{"openness_neg"}. Default: all three.}
}
\value{
A \code{SpatRaster} aligned to \code{mnt} with one layer per requested channel
(\code{svf} in 0..1, \code{openness_pos}/\code{openness_neg} in degrees), \code{NA} where the
DEM is \code{NA}.
}
\description{
Derives the \strong{sky-view factor} and \strong{openness} (positive and negative) of a
terrain model -- the micro-relief channels that carry the signal of a forest
track (platform depression, up-slope and down-slope embankments) far better
than the raw DEM. Thin GIS wrapper over the Rust kernel \code{\link[=rvt_svf_opns]{rvt_svf_opns()}}
(ported from the Relief Visualization Toolbox, Apache 2.0); the horizon sweep
runs in the crate, \code{terra} only lays the channels back onto the DEM grid.
}
\details{
\strong{Negative openness} is the openness of the \strong{inverted} DEM (it lights up
up-slope banks / ditches), computed here by running the kernel on \code{-z}.

The search radius is given in \strong{metres} and converted to pixels against the
DEM resolution. RVT's scale parameters are tuned for archaeological features a
few metres wide -- compatible with 3-6 m track platforms, but run a
\strong{sensitivity sweep} on a pilot massif before freezing them (spec 021 sec.6).
Feed a DEM at \strong{1 m or finer} for road work (see \code{\link[=acquire_desserte_lidar]{acquire_desserte_lidar()}}).
}
\examples{
mnt <- terra::rast(nrows = 30, ncols = 30, xmin = 0, xmax = 30,
                   ymin = 0, ymax = 30, crs = "EPSG:2154")
terra::values(mnt) <- 100 # terrain plat -> svf = 1, openness = 90
mr <- micro_relief(mnt, radius_m = 5)
names(mr)
}
\seealso{
\code{\link[=rvt_svf_opns]{rvt_svf_opns()}} (the Rust kernel), \code{\link[=acquire_desserte_lidar]{acquire_desserte_lidar()}} and
\code{\link[=qualifier_desserte]{qualifier_desserte()}} (consumers of a >= 1 m DEM).
}
