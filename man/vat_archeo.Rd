% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vat_archeo.R
\name{vat_archeo}
\alias{vat_archeo}
\title{VAT composite (Visualization for Archaeological Topography) from a DEM}
\usage{
vat_archeo(
  mnt,
  radius_m = 10,
  num_directions = 16L,
  sun_azimuth = 315,
  sun_elevation = 35,
  layers = vat_default_layers()
)
}
\arguments{
\item{mnt}{Digital terrain model as a single-layer \code{SpatRaster} (square cells;
the first layer is used if several).}

\item{radius_m}{Maximal horizon search radius in metres for the SVF/openness
channels. Default 10.}

\item{num_directions}{Number of azimuth directions swept. Default 16.}

\item{sun_azimuth}{Hillshade illumination azimuth in degrees (0 = North,
clockwise). Default 315.}

\item{sun_elevation}{Hillshade illumination elevation above the horizon, in
degrees. Default 35.}

\item{layers}{Layer specification passed to \code{\link[=blend_rvt]{blend_rvt()}}; its \code{name} fields
must be a subset of \code{"svf"}, \code{"openness_pos"}, \code{"slope"}, \code{"hillshade"}.
Default \code{\link[=vat_default_layers]{vat_default_layers()}}.}
}
\value{
A single-layer \code{SpatRaster} named \code{vat}, values in \verb{[0, 1]}, aligned to
\code{mnt}.
}
\description{
Builds the \strong{VAT} blend (Kokalj & Somrak 2019) from a digital terrain model:
it derives the four RVT channels -- \strong{sky-view factor} and \strong{positive
openness} via the Rust kernel \code{\link[=micro_relief]{micro_relief()}}, \strong{slope} and analytical
\strong{hillshade} via \code{terra} -- then fuses them with \code{\link[=blend_rvt]{blend_rvt()}} into a single
grayscale composite that reveals micro-relief (platforms, embankments,
ditches, mounds) far better than any single visualization.
}
\details{
Feed a DEM at \strong{1 m or finer} -- ideally the 0.5 m IGN LiDAR HD DTM (see
\code{\link[=acquire_desserte_lidar]{acquire_desserte_lidar()}}); coarser grids wash out the signal.

The default layer stack (\code{\link[=vat_default_layers]{vat_default_layers()}}) reproduces RVT's shipped
\code{VAT - Archaeological} preset (\code{blender_VAT.json}); both the blend machinery
and the constants are validated pixel-to-pixel against the RVT_py oracle (as
done for \code{\link[=rvt_svf_opns]{rvt_svf_opns()}}). Note that the slope layer is rendered on an
\strong{inverted} scale (steep = dark, per RVT's \code{normalize_image}) and that the
Overlay layer's opacity is neutralized (see \code{\link[=blend_rvt]{blend_rvt()}}). Override \code{layers}
to experiment.
}
\examples{
mnt <- terra::rast(nrows = 30, ncols = 30, xmin = 0, xmax = 30,
                   ymin = 0, ymax = 30, crs = "EPSG:2154")
terra::values(mnt) <- 100 + as.vector(terra::rowFromCell(mnt, 1:900)) * 0.2
vat <- vat_archeo(mnt, radius_m = 5)
names(vat)
}
\seealso{
\code{\link[=micro_relief]{micro_relief()}} (SVF/openness kernel), \code{\link[=blend_rvt]{blend_rvt()}} (the blend),
\code{\link[=vat_default_layers]{vat_default_layers()}} (default recipe).
}
