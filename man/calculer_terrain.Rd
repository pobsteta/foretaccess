% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrain.R
\name{calculer_terrain}
\alias{calculer_terrain}
\title{Pente et exposition depuis un MNT}
\usage{
calculer_terrain(mnt, methode = "Horn")
}
\arguments{
\item{mnt}{\code{SpatRaster} mono-couche, ou chemin de fichier.}

\item{methode}{Méthode de calcul : \code{"Horn"} (8 voisins) ou \code{"Evans"}
(4 voisins).}
}
\value{
Une liste de deux \code{SpatRaster} : \code{slope_pct} et \code{aspect_deg}, sur la
grille du MNT.
}
\description{
Dérive la \strong{pente en pourcentage} et l'\strong{exposition en degrés} (0–360 depuis
le nord, sens horaire) d'un modèle numérique de terrain, via
\code{\link[terra:terrain]{terra::terrain()}}.
}
\details{
La méthode par défaut est \strong{Horn} (8 voisins), celle de \code{terra}. Elle reste
configurable (\code{config$general$methode_pente}) pour permettre une réconciliation
ultérieure avec l'oracle Sylvaccess v3.6 sans refonte (spec 001 §10, décision 2).

Conventions de sortie :
\itemize{
\item \code{slope_pct} = \code{tan(pente_radians) * 100}.
\item \code{aspect_deg} vaut \code{NA} sur les cellules \strong{plates} (pente nulle), là où
\code{terra} renvoie conventionnellement 90.
\item Les cellules de \strong{bordure} valent \code{NA} dans les deux couches : le calcul
exige les 8 voisins. C'est un effet de bord documenté (spec 001 §8) ; les
comparaisons à l'oracle portent sur l'intérieur du raster.
}
}
\examples{
mnt <- terra::rast(system.file("extdata/toy/mnt.tif", package = "foretaccess"))
terr <- calculer_terrain(mnt)
terra::global(terr$slope_pct, "mean", na.rm = TRUE)
}
