% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_flux.R
\name{typer_desserte}
\alias{typer_desserte}
\title{Type a road network by wood flux}
\usage{
typer_desserte(graphe, seuils_flux, conversion_temporaire = NULL)
}
\arguments{
\item{graphe}{A \code{foretaccess_reseau_graphe} carrying a \code{flux} column on its
\code{troncons} (run \code{\link[=calculer_flux]{calculer_flux()}} first).}

\item{seuils_flux}{A named, ascending numeric vector of class lower bounds,
e.g. \code{c(tertiaire = 0, secondaire = 100, primaire = 500)}. Each tronçon is
assigned the highest class whose bound it reaches.}

\item{conversion_temporaire}{Optional list to convert part of a class into
temporary roads: \code{type} (source class), \code{proportion} (0..1 of that class's
length), \code{cible} (target label, default \code{"temporaire"}) and optional \code{zones}
(an \code{sf} of preferential areas).}
}
\value{
A \code{foretaccess_desserte_typee} object: \code{troncons} (\code{sf} with a \code{type}
column), \code{noeuds}, \code{sources} (if any), \code{recap} (length per type) and the
recall of \code{seuils_flux}.
}
\description{
Ports ForestRoadNetwork's "Road Type Determination": each tronçon is placed in
a road class by flux thresholds (high flux -> primary, ... , low -> tertiary).
Optionally, a share of a class's length is converted to temporary/winter roads,
preferentially inside dedicated zones.
}
