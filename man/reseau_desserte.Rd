% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_reseau.R
\name{reseau_desserte}
\alias{reseau_desserte}
\title{Design a forest-road network serving several parcels}
\usage{
reseau_desserte(
  pre,
  cout,
  parcelles,
  desserte_existante,
  heuristique = c("plus_proche", "plus_gros_volume", "aleatoire"),
  mode = c("glouton", "steiner"),
  skidding_m = 0,
  volume_champ = NULL,
  pondere_cout = FALSE,
  config = foretaccess_config(),
  graine = NULL
)
}
\arguments{
\item{pre}{A \code{foretaccess_preprocessing} object (DEM, terrain slope).}

\item{cout}{A \code{foretaccess_cout_construction} object (Lot 14): crossability.}

\item{parcelles}{An \code{sf} POLYGON of the areas to serve.}

\item{desserte_existante}{An \code{sf} LINESTRING of the network to connect to.}

\item{heuristique}{Ordering of parcels: \code{"plus_proche"} (closest first),
\code{"plus_gros_volume"} (largest volume first) or \code{"aleatoire"} (random).}

\item{mode}{Construction mode: \code{"glouton"} (greedy MTAP->STAP, default) or
\code{"steiner"} (minimum-spanning-tree approximation over the terminals, a
quality alternative at the cost of N^2 traces).}

\item{skidding_m}{Skidding distance (m): a parcel cell within it of a road is
served without building a road. \strong{Set this to the actual skidding/forwarding
distance of your operation} -- it is the dominant performance lever (see
\emph{Performance}). Left at \code{0}, every parcel cell that is not \emph{on} a road spawns
its own trace, which is both slow and over-connected.}

\item{volume_champ}{Optional name of the parcel volume column (for
\code{"plus_gros_volume"}); the column is \strong{rasterised onto the grid}, so each
cell inherits the parcel value and cells are then ordered by it. Pass a
\strong{density (m3/ha)}, \emph{not} a total: a per-parcel total rasterised onto every
cell would mis-rank parcels by area. When fed from nemeton's
\code{volume_mobilisable()}, use \code{unite = "m3_ha"} here -- the \strong{opposite} unit
to \code{\link[=calculer_flux]{calculer_flux()}}, which wants a per-parcel total (m3). Do not reuse the
same column for both.}

\item{pondere_cout}{If \code{TRUE}, weights the trace by the Lot 14 construction
cost surface (\code{cout$cout}, euros/m) instead of pure geometric distance; the
trace then minimises monetary cost. Default \code{FALSE} (SylvaRoad behaviour).}

\item{config}{A \code{foretaccess_config}; the solver settings live in
\code{config$desserte$trace}.}

\item{graine}{Optional integer seed for the \code{"aleatoire"} ordering.}
}
\value{
A \code{foretaccess_reseau} object: \code{lignes} (an \code{sf} LINESTRING of the
created roads, one feature per road, with creation \code{ordre}, \code{cout} and
planimetric \code{longueur} in m -- these follow the grid step by step; use
\code{\link[=contracter_lignes]{contracter_lignes()}} for a tidy vector rendering), \code{reseau} (a \code{SpatRaster}
of the whole
network, for Lot 17), \code{desserte} (the existing network, kept for the Lot 17
graph), \code{cout} (total), \code{connexe} and \code{raccorde} (two connectivity flags,
see \emph{Connectivity} below), \code{desservies} (a logical, one per parcel, CA-16.1)
and the recall of the \code{mode} and \code{heuristique}.
}
\description{
Builds a road network connecting N parcels to an existing road network, at
minimum cumulated construction cost, by the greedy MTAP heuristic: parcels are
ordered, then each is connected to the current network with the Lot 15
constrained solver, the new road growing the network for later parcels (reuse
-> tree). A parcel cell already within \code{skidding_m} of a road needs no road.
}
\section{Connectivity}{

Two booleans, with \strong{different} meanings -- read the right one:
\itemize{
\item \strong{\code{connexe}} -- does the \emph{whole} raster network (existing roads + created
roads) form a \strong{single} 8-connected component (CA-16.5)? This is dominated
by the \strong{existing} network's own fragmentation: a real reference network is
thousands of segments that do not touch at grid resolution, so \code{connexe} is
almost always \code{FALSE} on real data. \strong{A \code{FALSE} here does \emph{not} mean a
created road dangles} -- it usually just reflects a fragmented input.
\item \strong{\code{raccorde}} -- do the created roads add \strong{no new} connected component
relative to the existing network alone? \code{TRUE} iff every created road
attaches to the existing network (directly or through another created road);
a road left dangling would raise the component count. This is the flag that
answers \emph{"is every road I built actually connected?"} -- the one to surface
as a quality badge, not \code{connexe}.
}
}

\section{Performance}{

The cost is one A\* trace \strong{per parcel cell that builds a road}. Two things
govern it:
\itemize{
\item \strong{\code{skidding_m}} -- the number of traces. A parcel is a \emph{block} of cells; at
\code{skidding_m = 0} each cell not lying on a road spawns its own trace (hundreds
per hectare-sized parcel). Set \code{skidding_m} to the real skidding/forwarding
distance and a whole parcel is served by a \strong{single} trace as soon as one
road passes within reach -- collapsing the trace count to roughly one per
parcel cluster, and the runtime with it.
\item \strong{the trace itself} -- each trace is a genuine least-cost road \strong{alignment}
(direction/slope penalties, hairpin radius, profile checks), not a plain
shortest path. It is solved by an A\* \strong{bounded to the corridor} between the
parcel and its nearest network cell (a box padded in proportion to that
distance), with a fall-back to the full extent if no connection is found in
the corridor. A parcel connects to the \emph{nearest} network, so the optimum lies
in that corridor: the bound preserves the result for the realistic case and
turns a full-extent search into a local one.
}

Net effect on a departmental-scale run: a per-parcel trace drops from minutes
to milliseconds, and with a realistic \code{skidding_m} the whole greedy runs in
seconds to tens of seconds rather than minutes. The single-target trace of
\code{\link[=tracer_desserte]{tracer_desserte()}} is corridor-bounded the same way (its two waypoints define
the box). The window shrinks the search only when the two endpoints are close:
Steiner (\code{mode = "steiner"}) traces parcel-to-parcel across the whole extent, so
its windows are large and it stays roughly \code{N^2} (measured ~650 s for 4 parcels,
down from ~860 s) -- still reserved for small parcel counts.

\strong{Validity domain of the speed-up (read this before expecting seconds).} The
"tens of seconds" figure is \strong{conditional}, and the two levers compound:
\itemize{
\item \strong{\code{skidding_m} is the dominant one.} At \code{skidding_m = 0} \emph{every} parcel cell
off a road spawns its own A\* trace -- hundreds per hectare-scale parcel. That
count, not the per-trace cost, then dominates, and no windowing can offset it.
Measured on Chastel-Nouvel (30 parcels, ~302k cells) at \code{skidding_m = 0}: the
greedy runs in \strong{~17 min}, i.e. it does \strong{not} beat the pre-1.12 baseline --
the 1.12 "~11.5 min -> tens of seconds" figure was obtained \emph{with a realistic}
\code{skidding_m}, not at \code{0}. \strong{Set \code{skidding_m} to your real skidding/forwarding
distance}; it is not optional tuning.
\item \strong{Proximity to the existing network.} The corridor bound only shrinks the
search when a parcel is \emph{close} to the network (small box). Parcels far from
any road give large boxes and little gain, on top of the per-cell trace count.
}

So: \code{skidding_m} > 0 \strong{and} parcels not all far from the network -> seconds to
tens of seconds. \code{skidding_m = 0} with scattered, distant parcels -> minutes,
regardless of the windowing. Persist the returned network (do not recompute a
long greedy on every view).
}

