% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cable_find_thtv_tmax}
\alias{cable_find_thtv_tmax}
\title{Amorçage \verb{(Th, Tv, faisable)} par recherche sur grille sous la tension
admissible. Renvoie un vecteur de longueur 3 ; \code{faisable} vaut 1 si
\code{sqrt(Th^2 + Tv^2) <= tmax}, 0 sinon.}
\usage{
cable_find_thtv_tmax(tmax, w, eao, f, pas, d, h, lo, step)
}
\arguments{
\item{tmax}{Admissible cable tension (N).}

\item{w}{Weight of the cable over its whole length (N).}

\item{eao}{Young's modulus times the cable section (N).}

\item{f}{Gravity force of the load (N).}

\item{pas}{Arc-length position of the load used for the grid seed (m).}

\item{d}{Horizontal span between supports (m).}

\item{h}{Altitude difference between supports (m).}

\item{lo}{Unstretched cable length over the span (m).}

\item{step}{Grid step in tension (N).}
}
\value{
A length-3 numeric vector \code{c(Th, Tv, faisable)}.
}
\description{
Amorçage \verb{(Th, Tv, faisable)} par recherche sur grille sous la tension
admissible. Renvoie un vecteur de longueur 3 ; \code{faisable} vaut 1 si
\code{sqrt(Th^2 + Tv^2) <= tmax}, 0 sinon.
}
