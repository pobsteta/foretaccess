% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{desserte_trace}
\alias{desserte_trace}
\title{Trace a forest road through mandatory waypoints (road-design A\* solver, Lot 15b).}
\usage{
desserte_trace(
  alt,
  obs,
  obs2,
  local_slope,
  zone,
  nr,
  nc,
  waypoints,
  bufgoal,
  csize,
  min_slope,
  max_slope,
  penalty_xy,
  penalty_z,
  max_diff_z,
  d_neighborhood,
  angle_hairpin,
  lmax_ab_sl,
  radius,
  prop_sl_max,
  max_slope_hairpin,
  tal,
  modhair
)
}
\arguments{
\item{alt}{Elevation values (row-major, m).}

\item{obs}{Obstacle mask (1 = blocked / non-crossable), row-major.}

\item{obs2}{Excess cross-slope mask (1 = terrain slope over \code{trans_slope_all}).}

\item{local_slope}{Fraction (0..1) of the neighbourhood with steep cross-slope.}

\item{zone}{Passable mask (1 = passable) for the inverse-distance heuristic.}

\item{nr}{Number of raster rows.}

\item{nc}{Number of raster columns.}

\item{waypoints}{0-based flattened cell indices to visit, in order.}

\item{bufgoal}{Finish tolerance around the final goal (m).}

\item{csize}{Cell size (m).}

\item{min_slope}{Minimum road grade (percent).}

\item{max_slope}{Maximum road grade (percent).}

\item{penalty_xy}{Turn (direction-change) penalty (m per 180 deg).}

\item{penalty_z}{Slope-change ("wave") penalty.}

\item{max_diff_z}{Max elevation gap between road and terrain (m).}

\item{d_neighborhood}{Neighbourhood radius (m).}

\item{angle_hairpin}{Angle above which a turn is a hairpin (deg).}

\item{lmax_ab_sl}{Max road length with excess cross-slope (m).}

\item{radius}{Turning radius for trucks (m).}

\item{prop_sl_max}{Max local steep-cross-slope fraction at a hairpin.}

\item{max_slope_hairpin}{Slope tolerance parameter for the hairpin limit angle.}

\item{tal}{Hairpin limit-angle tuning parameter.}

\item{modhair}{Minimum-spacing-between-hairpins parameter.}
}
\value{
A list: \code{path} (1-based flattened cell indices), \code{cost}, \code{feasible}.
}
\description{
Ports SylvaRoad's \code{Astar_force_wp}: A\* on the disc-neighbourhood graph, with
geometric transition cost plus parabolic direction/slope penalties, hairpin
handling (turning \code{radius}, limit angle), longitudinal-profile control
(\code{check_profile}) and self-intersection avoidance (spec 015 Sec. 4). The
neighbourhood table is (re)built internally from the DEM and obstacle mask.
}
\details{
All grids are row-major and flattened; \code{waypoints} are 0-based flattened cell
indices (>= 2), the first the origin and the last the final goal.
}
