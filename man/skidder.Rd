% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skidder.R
\name{skidder}
\alias{skidder}
\title{Moteur d'accessibilité skidder (débusqueur)}
\usage{
skidder(
  pre,
  config = foretaccess_config(),
  trajets_depuis = NULL,
  write_dir = NULL,
  bord = NULL
)
}
\arguments{
\item{pre}{Objet \code{foretaccess_preprocessing} issu de \code{\link[=preprocess]{preprocess()}}.}

\item{config}{Objet \code{\link[=foretaccess_config]{foretaccess_config()}}.}

\item{trajets_depuis}{Cellules (indices ou points \code{sf}) pour lesquelles
reconstruire le trajet de traînage vers la desserte. \code{NULL} (défaut) : aucun.}

\item{write_dir}{Répertoire d'écriture des rasters en GeoTIFF/COG, ou \code{NULL}.}

\item{bord}{Côtés ouverts de la fenêtre, quand \code{pre} n'est qu'une \strong{tuile} d'un
territoire plus vaste (voir \code{\link[=certifier_propagation]{certifier_propagation()}}). \code{NULL} (défaut) : \code{pre}
couvre tout le territoire, le résultat est exact partout. Sinon, la sortie porte
une couche \code{certifie} (spec 007 §4.3).}
}
\value{
Un objet de classe \code{foretaccess_skidder} :
\describe{
\item{\code{accessibilite}}{\code{SpatRaster} catégoriel : \code{parcourable},
\code{accessible}, \code{non_accessible}, \code{hors_foret}. \code{NA} = indéterminé.}
\item{\code{distance_treuillage}}{distance \strong{3D} de treuillage (m), 0 sinon.}
\item{\code{distance_trainage_foret}}{distance de traînage en forêt (m).}
\item{\code{distance_trainage_piste}}{distance sur piste jusqu'à une route (m).}
\item{\code{distance_debardage}}{somme des trois précédentes.}
\item{\code{allocation}}{cellule de desserte de rattachement.}
\item{\code{trajet}}{\code{sf} de \code{LINESTRING}, ou \code{NULL}.}
\item{\code{certifie}}{\code{SpatRaster} logique, ou \code{NULL} si \code{bord} l'est.}
\item{\code{recap}}{\code{data.frame} des surfaces et volumes par classe.}
\item{\code{grid}, \code{config}, \code{fichiers}}{comme au Lot 1.}
}
}
\description{
Applique les règles Sylvaccess v3.6 au jeu de rasters produit par
\code{\link[=preprocess]{preprocess()}} : circulation libre sous le seuil de pente, treuillage
au-delà, et distances de débardage. Aucune I/O : le moteur consomme l'objet du
Lot 1 (ADR-004) et tous ses seuils viennent de \code{config} (ADR-003).
}
\details{
Deux mécanismes coexistent, et ils n'ont pas la même nature :
\itemize{
\item le \strong{traînage} est un plus court chemin sur la surface de coût
(\code{\link[=propager_cout]{propager_cout()}}, \code{\link[=surface_cout_skidder]{surface_cout_skidder()}}) ;
\item le \strong{treuillage} est un balayage radial en ligne droite (\code{\link[=treuiller]{treuiller()}}).
}

L'option de modélisation \code{1} (défaut v3.6, « limiter l'impact sur le sol »)
\strong{privilégie le treuillage} : une cellule treuillable l'est, même si l'engin
pourrait y rouler. L'option \code{2} n'est pas implémentée.

Le treuillage se fait en \strong{trois passes}, comme dans Sylvaccess :
depuis les routes, depuis les pistes, puis — c'est le point qu'on oublie —
depuis le \strong{contour de la zone où l'engin a roulé} (\code{skid_debusq_contour}).
La machine entre en forêt, s'arrête au bord du terrain roulable, et treuille
de là en \strong{emportant la distance déjà parcourue} : le critère de choix porte
alors sur le \strong{total} traînage + câble, non sur la seule longueur de câble.
Cette troisième passe est purement additive — elle ne corrige jamais une
cellule que les deux premières ont atteinte.
}
\section{Écarts assumés avec Sylvaccess v3.6}{

La hiérarchie route / piste est réduite à deux niveaux (\code{route} et \code{dfci}
comptent comme routes), et l'option de modélisation 2 n'est pas implémentée.
Voir \code{specs/002-skidder.md}.
}

\examples{
toy <- system.file("extdata/toy", package = "foretaccess")
pre <- preprocess(file.path(toy, "mnt.tif"), file.path(toy, "desserte.gpkg"),
                  file.path(toy, "foret.gpkg"))
sk <- skidder(pre)
sk$recap
}
