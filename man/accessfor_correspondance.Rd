% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessfor.R
\name{accessfor_correspondance}
\alias{accessfor_correspondance}
\title{Map the IGN ACCESSFOR classes onto \code{classes_debardage()}}
\usage{
accessfor_correspondance(config = foretaccess_config())
}
\arguments{
\item{config}{A \code{foretaccess_config}. Its \code{skidder$classes_distance_m} must
equal the ACCESSFOR bands. Default \code{\link[=foretaccess_config]{foretaccess_config()}}.}
}
\value{
A \code{data.frame} with one row per \code{classes_debardage()} value:
\code{fa_value} (integer, the raster code), \code{fa_classe} (label), \code{accessfor_class}
(integer ACCESSFOR code, \code{NA} for \code{hors_foret}), \code{accessfor_cat} (indicative
ASCII label -- do not join on it).
}
\description{
The IGN publishes a national forest-accessibility layer (project \strong{ACCESSFOR},
edition 2025-01-01) over WFS: polygonal \code{acces_skidder} / \code{acces_porteur}
layers with an integer \code{class} and a \code{cat} label. On the target department
(48, Chastel-Nouvel) the observed class domain matches \code{\link[=classes_debardage]{classes_debardage()}}
band for band -- both descend from Sylvaccess. This function returns the
explicit crosswalk so a comparison joins on the \strong{integer} code, never on the
label (\code{cat} carries accents and punctuation that must not be matched on).
}
\details{
The ACCESSFOR bands are \strong{frozen} at \verb{0, 250, 500, 1000, 1500, 2000} m (the
Sylvaccess defaults). If \code{config} sets different
\code{skidder$classes_distance_m}, the crosswalk is undefined and the function
errors rather than align mismatched bands silently.
}
\section{Correspondence}{

\tabular{lll}{
ACCESSFOR \code{class} \tab \code{classes_debardage()} \tab meaning \cr
3, 4, 5, 6, 7, 8 \tab 1, 2, 3, 4, 5, 6 \tab bands 0-250 ... > 2000 m \cr
1 \tab \code{k+1} \tab inaccessible \cr
2 \tab \code{k+2} \tab inexploitable (harvest slope exceeded) \cr
-- \tab \code{k+3} \tab hors_foret (no ACCESSFOR code -- outside its forest mask) \cr
}
where \code{k = length(config$skidder$classes_distance_m)} (6 by default). ACCESSFOR
has \strong{no} \code{hors_foret} code: its polygons \emph{are} the forest, so \code{hors_foret}
maps to "outside the ACCESSFOR mask" and is excluded from any comparison.
}

\examples{
accessfor_correspondance()
}
\seealso{
\code{\link[=classes_debardage]{classes_debardage()}}.
}
