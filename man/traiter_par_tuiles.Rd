% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mosaique.R
\name{traiter_par_tuiles}
\alias{traiter_par_tuiles}
\title{Traiter une emprise par tuiles}
\usage{
traiter_par_tuiles(
  pre,
  config = foretaccess_config(),
  moteur = skidder,
  write_dir = NULL,
  quiet = FALSE,
  couches = NULL
)
}
\arguments{
\item{pre}{Objet \code{foretaccess_preprocessing} couvrant toute l'emprise.}

\item{config}{Objet \code{\link[=foretaccess_config]{foretaccess_config()}}. Les paramètres de tuilage vivent dans
\code{config$general} : \code{tuile_m}, \code{halo_initial_m}, \code{halo_max_m}.}

\item{moteur}{Fonction moteur, de signature \verb{(pre, config, bord)}. Défaut \code{\link[=skidder]{skidder()}}.}

\item{write_dir}{Répertoire d'écriture des COG recomposés, ou \code{NULL}.}

\item{quiet}{Supprime la progression.}

\item{couches}{Noms des couches de sortie du \code{moteur} à recomposer. \code{NULL} (défaut) :
celles du skidder. Pour le porteur, passer \code{.couches_porteur()}.}
}
\value{
Un objet de classe \code{foretaccess_mosaique} :
\describe{
\item{\code{accessibilite}, \verb{distance_*}, \code{allocation}}{les couches du moteur,
recomposées sur toute l'emprise.}
\item{\code{certifie}}{\code{SpatRaster} logique. Gardé en mémoire, \strong{pas écrit} : les
cellules non certifiées se lisent déjà comme \code{NA} dans les couches.}
\item{\code{recap}}{\code{data.frame} agrégé, \code{indetermine} compris.}
\item{\code{tuiles}}{\code{data.frame} : halo final et cellules non certifiées, par tuile.}
\item{\code{indetermine_ha}}{surface non certifiée, toujours reportée.}
\item{\code{grid}, \code{config}, \code{fichiers}}{comme au Lot 1.}
}
}
\description{
Découpe l'emprise, applique un moteur à chaque tuile sur une fenêtre élargie d'un
halo, \strong{certifie} le résultat cellule par cellule (spec 007 §4.3) et recompose une
mosaïque. Le résultat est \strong{identique} à celui du moteur appliqué d'un seul bloc,
partout où il est certifié.
}
\details{
Le halo \strong{double} tant que des cellules de la tuile restent non certifiées, jusqu'à
\code{halo_max_m}. Au-delà, ces cellules sortent en \code{indetermine} — la classe que
\code{\link[=recapituler]{recapituler()}} produit déjà pour les bordures de pente — et un avertissement le dit.
Elles ne sont \strong{jamais} rangées dans \code{non_accessible} : le doute se déclare.

\code{pre} doit couvrir toute l'emprise. Ses rasters peuvent être \strong{adossés à des
fichiers} (\verb{preprocess(write_dir = …)}) : \code{terra} n'en charge alors que la fenêtre
de chaque tuile, ce qui rend le traitement possible là où le mono-bloc ne tiendrait
pas en mémoire — le véritable obstacle du passage à l'échelle, avant le temps de calcul.

L'\code{allocation} porte un indice de cellule dans la grille \strong{globale}, jamais celle de
la tuile : sans cette remise à l'échelle, deux tuiles alloueraient le même identifiant
à deux dessertes différentes.
}
\examples{
toy <- system.file("extdata/toy", package = "foretaccess")
pre <- preprocess(file.path(toy, "mnt.tif"), file.path(toy, "desserte.gpkg"),
                  file.path(toy, "foret.gpkg"))
cfg <- foretaccess_config(general = list(tuile_m = 150, halo_initial_m = 50))
mo <- traiter_par_tuiles(pre, cfg, quiet = TRUE)
mo$recap
}
\seealso{
\code{\link[=decouper_emprise]{decouper_emprise()}}, \code{\link[=certifier_propagation]{certifier_propagation()}}, \code{\link[=skidder]{skidder()}}
}
