% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cable_find_lomin}
\alias{cable_find_lomin}
\title{Cherche la longueur à vide minimale \code{Lo} telle que la tension du câble, la
charge au milieu, atteigne \code{tmax}, puis vérifie la garde au sol sur toute la
travée. Renvoie un vecteur \code{c(faisable, Lo, Th, Tv, Tcalc, F)} ; \code{faisable}
vaut 1 ou 0. Sans supports intermédiaires (Lot 4c).}
\usage{
cable_find_lomin(
  d,
  h,
  xup,
  zup,
  fact,
  alts,
  f_o,
  tmax,
  q1,
  q2,
  q3,
  eao,
  hline_min,
  hline_max,
  csize
)
}
\arguments{
\item{d}{Horizontal span between supports (m).}

\item{h}{Altitude difference between supports (m).}

\item{xup}{Horizontal position of the upper support (m).}

\item{zup}{Altitude of the upper support (m).}

\item{fact}{Direction of the line (+1 or -1).}

\item{alts}{Terrain altitudes under the line, sampled every 0.5 m (m).}

\item{f_o}{Gravity force of the load and carriage (N).}

\item{tmax}{Admissible cable tension (N).}

\item{q1}{Linear mass of the skyline (kg/m).}

\item{q2}{Linear mass of the mainline / traction cable (kg/m).}

\item{q3}{Linear mass of the return cable (kg/m).}

\item{eao}{Young's modulus times the cable section (N).}

\item{hline_min}{Minimum ground clearance of the cable (m).}

\item{hline_max}{Maximum height of the cable (m).}

\item{csize}{Sweep step of the load position (m).}
}
\value{
A length-6 numeric vector \code{c(faisable, Lo, Th, Tv, Tcalc, F)}.
}
\description{
Amorçage substitué aux tables Sylvaccess (\verb{(Th, Tv) = (0.9*tmax, 0.1*tmax)},
\code{Lo = corde + réserve}) : choix de performance, pas de correction.
}
