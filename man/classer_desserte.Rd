% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_classer.R
\name{classer_desserte}
\alias{classer_desserte}
\title{Classify detected linear features (desserte, skid trail, firebreak...)}
\usage{
classer_desserte(
  traces,
  reference = NULL,
  parcellaire = NULL,
  sous_type_parcelle = c("section", "border"),
  stations = NULL,
  ndvi = NULL,
  tpi = NULL,
  ...
)
}
\arguments{
\item{traces}{Detected network: path or \code{sf} of lines (the output of
\code{\link[=detecter_desserte]{detecter_desserte()}}).}

\item{reference}{Reference network (\code{sf}/\code{sfc}, e.g. BD TOPO): what it carries
is a desserte. \code{NULL} to judge every line on its structure alone.}

\item{parcellaire}{Forest compartment boundaries (\code{sf}/\code{sfc}) or \code{NULL}.}

\item{sous_type_parcelle}{OSM sub-type of those boundaries: \code{"section"}
(management units -- boundaries marked on the ground) or \code{"border"}
(cadastral property limits). Passed \strong{explicitly} to \code{dessertR}, which
otherwise emits a notice: the value cannot be read off the geometry.}

\item{stations}{Per-station measurements (\code{sf}/\code{data.frame} with a \code{troncon}
column, from \code{dsr_measure()}) or \code{NULL}.}

\item{ndvi}{\code{SpatRaster} of NDVI, or \code{NULL} -- without it, road/track is never
decided and \code{pare_feu} is never posted.}

\item{tpi}{\code{SpatRaster} of topographic position, or \code{NULL}.}

\item{...}{Passed to \code{dessertR::dsr_classer()}.}
}
\value{
The input \code{sf} with \code{CLASSE}, \code{CLASSE_CONF}, \code{CLASSE_MOTIF} and
\code{OSM_TAGS}. Attribute \code{disponible}: \code{FALSE} when \code{dessertR} is missing or
the classification failed -- the columns are then all \code{NA} and a \strong{warning}
is emitted. \code{NA} classes mean \emph{"not classified"}, never \emph{"nothing found"}.
}
\description{
Wraps \code{dessertR::dsr_classer()}: labels each line of a \strong{detected} network
with \code{CLASSE}, \code{CLASSE_CONF} (share of informed criteria that agree),
\code{CLASSE_MOTIF} (which criteria voted) and \code{OSM_TAGS} (a \emph{proposed} tagging,
never uploaded).
}
\details{
\strong{Why it lives here.} Callers depend on \code{foretaccess}, never on its engine.
The app used to call \code{dessertR::dsr_classer()} directly, wrapped in a
\code{tryCatch()} that returned \code{NULL}: without \code{dessertR} installed the
classification vanished \strong{silently}. Here the unavailability is a warning and
an attribute, not an absence.

\strong{Geometry.} \code{dsr_classer()} requires \code{LINESTRING}; BD TOPO and most
detection outputs are \code{MULTILINESTRING}. The recast is done here, once.

\strong{The criteria you do not pass are unknown, not false.} \code{stations}
(\code{\link[=acquire_desserte_lidar]{acquire_desserte_lidar()}}'s measurement), \code{ndvi} and \code{tpi} each switch on a
family of criteria; without them \code{dessertR} declares them unknown and
\code{CLASSE_CONF} drops. \strong{Always display \code{CLASSE_CONF} next to \code{CLASSE}.}
}
\examples{
det <- sf::st_sf(geometry = sf::st_sfc(
  sf::st_linestring(rbind(c(0, 0), c(100, 0))), crs = 2154))
cl <- classer_desserte(det)      # sans dessertR : colonnes a NA, avertissement
attr(cl, "disponible")
}
\seealso{
\code{\link[=detecter_desserte]{detecter_desserte()}}, \code{\link[=dessertR_disponible]{dessertR_disponible()}}, \code{specs/030}.
}
