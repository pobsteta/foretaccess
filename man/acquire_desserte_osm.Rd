% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte-osm.R
\name{acquire_desserte_osm}
\alias{acquire_desserte_osm}
\title{Acquiert la desserte candidate depuis OpenStreetMap (spec 028)}
\usage{
acquire_desserte_osm(
  aoi,
  crs = 2154,
  cache_dir = tempdir(),
  overwrite = FALSE,
  types = .TYPES_HIGHWAY_DESSERTE,
  politique_cache = "reacquerir"
)
}
\arguments{
\item{aoi}{Objet \code{sf}/\code{sfc} d'emprise, dans le CRS cible.}

\item{crs}{Code EPSG de sortie. Défaut 2154.}

\item{cache_dir}{Répertoire de cache.}

\item{overwrite}{Re-télécharger même si le cache existe. Défaut \code{FALSE}.}

\item{types}{Valeurs de \code{highway} retenues. Défaut : \code{track},
\code{unclassified}, \code{service}.}

\item{politique_cache}{Que faire d'un cache produit avec \strong{d'autres
paramètres} ? \code{"reacquerir"} (défaut) refait l'acquisition, \code{"avertir"} sert
le cache en nommant ce qui diverge, \code{"echouer"} interrompt, \code{"ignorer"}
désactive le contrôle. Un cache \strong{sans provenance} (antérieur à la v1.29.0)
compte comme divergent. Cf. \code{\link[=cache_utilisable]{cache_utilisable()}} et \code{specs/027}.}
}
\value{
Un \code{sf} de lignes avec \code{source = "osm"}, \code{highway}, et les attributs
\code{tracktype}, \code{surface}, \code{access} quand ils sont présents dans le flux.
}
\description{
Source \strong{complémentaire} de la BD TOPO, jamais substitutive. Les tronçons
rendus sont des \strong{candidats} : ils doivent passer \code{\link[=qualifier_desserte]{qualifier_desserte()}}
avant d'entrer dans une conception de réseau.
}
\details{
\code{path} et \code{tertiary} sont exclus par défaut. Sur l'AOI oracle, \code{path}
représente 14,09 km hors corridor BD TOPO mais relève surtout de la randonnée,
et une \code{tertiary} absente de la BD TOPO est plus probablement un décalage de
saisie qu'une découverte.

\strong{Overpass injoignable lève une erreur}, jamais une couche vide : un appelant
confondrait sinon « le serveur refuse » et « il n'y a rien ici ». C'est la
confusion qui a masqué l'absence de DFCI pendant une journée entière.
}
\seealso{
\code{\link[=comparer_desserte_osm]{comparer_desserte_osm()}}, \code{\link[=acquire_desserte]{acquire_desserte()}} (la référence),
\code{specs/028}.
}
