% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volume.R
\name{volume_depuis_p1}
\alias{volume_depuis_p1}
\title{Rasterise a per-hectare volume layer onto the DTM grid}
\usage{
volume_depuis_p1(p1, mnt, champ = "P1", fun = "mean")
}
\arguments{
\item{p1}{Units carrying the per-hectare volume: path to a vector file or an
\code{sf} of polygons (the output of \code{nemeton::indicateur_p1_volume()}, or any
equivalent layer).}

\item{mnt}{DTM defining the target grid: \code{SpatRaster} or path. Must share the
CRS of \code{p1} (no implicit reprojection, ADR-004).}

\item{champ}{Name of the m3/ha column to rasterise. Default \code{"P1"} (the column
Nemeton writes).}

\item{fun}{How to combine when several units cover one cell. Default \code{"mean"}
-- correct for a density; overlapping units are unusual but must not be
summed. Passed to \code{\link[terra:rasterize]{terra::rasterize()}}.}
}
\value{
A single-layer \code{SpatRaster} named \code{volume}, aligned on \code{mnt}, ready for
\code{preprocess(volume = )}. Cells no unit covers are \code{NA} (no wood, not zero).
}
\description{
The cable engine and the line selection (Lot 5) read a \strong{standing-volume
raster} from \code{pre$volume}, in m3/ha per cell: \code{\link[=potentiel_cable]{potentiel_cable()}} sums it over
the forest cells a line covers to get the line volume and the cable production
index (IPC = volume / length). ForetAccess does \strong{not} compute that volume --
it is an input.
}
\details{
Its natural source is a forest inventory or a LiDAR canopy-height model, e.g.
Nemeton's \strong{P1} indicator (\code{nemeton::indicateur_p1_volume()}), which returns
an \code{sf} of units carrying a volume in \strong{m3/ha}. \code{volume_depuis_p1()} bridges
that \code{sf} to the grid the engines expect. It takes no dependency on Nemeton:
it rasterises whatever per-hectare field you give it, wherever it comes from.
}
\section{Unites}{

\code{champ} \strong{must be a density in m3/ha}, not an absolute volume per unit. The
cable multiplies each cell by \code{aire_cellule / 10000} to turn m3/ha back into
m3; a field in absolute m3 would inflate the result by the number of cells per
unit. This is the same convention as \code{vol_ha.tif} in the Sylvaccess ColduPre
set.
}

\examples{
\dontrun{
# Volume from Nemeton's P1 indicator (inventory or LiDAR CHM), then cable.
p1  <- nemeton::indicateur_p1_volume(parcelles, chm = mnh_lidar)
vol <- volume_depuis_p1(p1, mnt)
pre <- preprocess(mnt, desserte, foret, volume = vol)
ca  <- potentiel_cable(pre, departs = places)
}
}
\seealso{
\code{\link[=preprocess]{preprocess()}} (consumes the raster), \code{\link[=potentiel_cable]{potentiel_cable()}} (sums it per
line into volume and IPC).
}
