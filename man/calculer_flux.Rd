% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_flux.R
\name{calculer_flux}
\alias{calculer_flux}
\title{Accumulate wood flux over a road-network graph}
\usage{
calculer_flux(graphe, parcelles, volume_champ = "volume", densite_sources = 5)
}
\arguments{
\item{graphe}{A \code{foretaccess_reseau_graphe} (Lot 17a).}

\item{parcelles}{An \code{sf} POLYGON of the harvested parcels, carrying a volume.}

\item{volume_champ}{Name of the parcel volume column (default \code{"volume"}). It
must hold a \strong{total volume per parcel} (m3), \emph{not} a density: the value is
split equally across the parcel's source points and accumulated down the
tree, so a density (m3/ha) would under-estimate the flux by roughly the
parcel area -- silently, no error. When fed from nemeton's
\code{volume_mobilisable()}, use \code{unite = "m3_total"}. This is the \strong{opposite}
unit to \code{\link[=reseau_desserte]{reseau_desserte()}}'s \code{volume_champ} (a per-cell density).}

\item{densite_sources}{Source points per hectare (default 5); at least one
point is seeded per parcel regardless.}
}
\value{
The \code{graphe} with a \code{flux} column added to \code{troncons} (accumulated
volume) and a \code{sources} \code{sf} POINT (seeded points with their \code{volume}).
}
\description{
Ports ForestRoadNetwork's "Wood Flux Determination": source points are seeded
in each harvested parcel (at least one per parcel, whatever the density), each
injects its share of the parcel volume, and the volume flows down the network
along the least-cost path to the nearest outlet, accumulating on every tronçon
it crosses. As the network is a tree rooted on the existing roads, that path
is unique and the accumulation is a subtree sum.
}
