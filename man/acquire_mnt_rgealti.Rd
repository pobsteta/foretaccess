% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acquire-ign.R
\name{acquire_mnt_rgealti}
\alias{acquire_mnt_rgealti}
\title{Acquiert un MNT RGE ALTI depuis les \strong{dalles départementales} (Géoservices)}
\usage{
acquire_mnt_rgealti(
  aoi,
  dep,
  res_m = 5,
  crs = 2154,
  cache_dir = tempdir(),
  overwrite = FALSE,
  politique_cache = "reacquerir"
)
}
\arguments{
\item{aoi}{Objet \code{sf}/\code{sfc} d'emprise, dans le CRS cible.}

\item{dep}{Code du département sur deux caractères (ex. \code{"48"}).}

\item{res_m}{Résolution du produit RGE ALTI : 5 ou 1. Défaut 5.}

\item{crs}{Code EPSG de sortie. Défaut 2154.}

\item{cache_dir}{Répertoire de cache.}

\item{overwrite}{Re-télécharger même si le cache existe. Défaut \code{FALSE}.}

\item{politique_cache}{Que faire d'un cache produit avec \strong{d'autres
paramètres} ? \code{"reacquerir"} (défaut) refait l'acquisition, \code{"avertir"} sert
le cache en nommant ce qui diverge, \code{"echouer"} interrompt, \code{"ignorer"}
désactive le contrôle. Un cache \strong{sans provenance} (antérieur à la v1.29.0)
compte comme divergent. Cf. \code{\link[=cache_utilisable]{cache_utilisable()}} et \code{specs/027}.}
}
\value{
Le chemin du raster \code{mnt_rgealti.tif} écrit en cache.
}
\description{
Alternative saine au RGE ALTI servi par WMS, qui rend un MNT \strong{blocky} (blocs
plats à marches) dont la pente est fausse. C'est le produit que prescrit la
notice ACCESSFOR (rapport février 2025, annexe p. 50) : \emph{« Livraison d'un MNT
provenant du RGE Alti 5m converti en 32bit »}, téléchargé par département.
}
\details{
L'archive départementale (~450 Mo en \verb{.7z} pour le 5 m) est téléchargée une
fois et mise en cache ; seules les \strong{dalles couvrant l'AOI} en sont extraites,
puis mosaïquées et découpées. L'extraction requiert \code{py7zr} (Python) ou un
binaire \verb{7z} sur le \code{PATH} -- l'archive Géoservices n'est pas lisible autrement.

Sur la même AOI, ce produit donne une distribution de pente quasi identique au
MNT LiDAR HD (médiane 39,96 \% contre 40,99 \%), là où la variante WMS donnait
une médiane de 18,89 \% et un maximum de 382 \%.
}
\seealso{
\code{\link[=acquire_mnt]{acquire_mnt()}} (LIDAR HD, source par défaut).
}
