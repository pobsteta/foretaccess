% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leastcost.R
\name{chemin_optimal}
\alias{chemin_optimal}
\title{Trajet optimal d'une cellule vers sa source}
\usage{
chemin_optimal(propagation, depuis)
}
\arguments{
\item{propagation}{Objet \code{foretaccess_propagation} issu de \code{\link[=propager_cout]{propager_cout()}}.}

\item{depuis}{Cellule de départ : indice de cellule (entier), ou objet \code{sf} de
points (chaque point donne un trajet).}
}
\value{
Un objet \code{sf} de \code{LINESTRING} dans le CRS de la propagation, avec les
colonnes \code{cellule} (cellule de départ), \code{source} (identifiant alloué) et
\code{cout} (coût cumulé). Les cellules non atteintes sont ignorées, avec un
avertissement.
}
\description{
Remonte le raster de prédécesseurs produit par \code{\link[=propager_cout]{propager_cout()}} pour
reconstituer le chemin de moindre coût, de \code{depuis} jusqu'à la source qui lui a
été allouée.
}
