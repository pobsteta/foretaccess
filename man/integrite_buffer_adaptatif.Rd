% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_integrite.R
\name{integrite_buffer_adaptatif}
\alias{integrite_buffer_adaptatif}
\title{Élargissement adaptatif de l'emprise pour distinguer l'effet de bord (spec 025)}
\usage{
integrite_buffer_adaptatif(
  aoi,
  acquerir = NULL,
  buffer_initial = 100,
  buffer_max = 3000,
  facteur = 2,
  gain_min = 0.05,
  ...
)
}
\arguments{
\item{aoi}{Emprise stricte (\code{sf}/\code{sfc}).}

\item{acquerir}{Fonction \verb{function(emprise)} rendant la desserte sur cette
emprise. Défaut : \code{\link[=acquire_desserte]{acquire_desserte()}} avec les réglages courants.}

\item{buffer_initial, buffer_max}{Bornes du balayage (m). Défauts 100 et 3000 --
au-delà on téléchargerait un département pour rattacher une piste.}

\item{facteur}{Multiplicateur d'une itération à l'autre. Défaut 2.}

\item{gain_min}{Arrêt quand la décroissance relative de \code{L(b)} passe sous ce
seuil. Défaut 0,05 (5 \%).}

\item{...}{Passé à \code{\link[=verifier_integrite_desserte]{verifier_integrite_desserte()}}.}
}
\value{
Un objet \code{foretaccess_integrite} du dernier buffer, dont \code{courbe} est
un \code{data.frame} (\code{buffer_m}, \code{n_infractions}, \code{longueur_infraction_m}).
}
\description{
Une piste connectée à une route située \strong{hors de l'AOI} paraît orpheline :
c'est un artefact de découpe, pas un défaut de donnée. Plutôt que de deviner un
buffer, on le fait \strong{converger}.
}
\details{
Le point qui fait la validité de la méthode : la longueur en infraction est
mesurée \strong{sur l'AOI stricte} à chaque itération. Mesurée sur l'emprise
élargie, elle augmenterait mécaniquement (plus de réseau, donc plus
d'infractions) et la suite ne convergerait jamais.

La \strong{décroissance de \code{L(b)} sépare les causes} : ce qui disparaît en
élargissant était un effet de bord ; ce qui résiste à un buffer large est
topologique ou réel. La courbe est le diagnostic, pas un sous-produit.
}
\seealso{
\code{\link[=verifier_integrite_desserte]{verifier_integrite_desserte()}}, \code{specs/025}.
}
