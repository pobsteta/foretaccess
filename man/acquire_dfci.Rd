% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acquire-osm.R
\name{acquire_dfci}
\alias{acquire_dfci}
\title{Acquiert le réseau DFCI depuis OpenStreetMap}
\usage{
acquire_dfci(
  aoi,
  crs = 2154,
  cache_dir = tempdir(),
  overwrite = FALSE,
  politique_cache = "reacquerir"
)
}
\arguments{
\item{aoi}{Objet \code{sf}/\code{sfc} d'emprise, dans le CRS cible.}

\item{crs}{Code EPSG de sortie. Défaut 2154.}

\item{cache_dir}{Répertoire de cache.}

\item{overwrite}{Re-télécharger même si le cache existe. Défaut \code{FALSE}.}

\item{politique_cache}{Que faire d'un cache produit avec \strong{d'autres
paramètres} ? Défaut \code{"reacquerir"}. Voir \code{\link[=cache_utilisable]{cache_utilisable()}} et
\code{specs/027}.}
}
\value{
Un objet \code{sf} de lignes DFCI avec un champ \code{ref}, ou un \code{sf} vide si
aucune piste DFCI n'est trouvée.
}
\description{
Récupère les pistes de défense des forêts contre l'incendie (DFCI) portant une
référence OSM \code{ref:FR:DFCI} (ou les alias \code{ref:dfci} / \code{dfci_ref}) au sein de
l'emprise. Ces lignes servent à poser le flag \code{dfci} (\code{CL_DFCI}) sur la
desserte via \code{\link[=flag_dfci]{flag_dfci()}} — la source du camion DFCI (spec 006). C'est la
« source dédiée » laissée ouverte en phase 1 (spec 010 §10.2).
}
\seealso{
\code{\link[=flag_dfci]{flag_dfci()}}, \code{\link[=acquire_desserte]{acquire_desserte()}}
}
