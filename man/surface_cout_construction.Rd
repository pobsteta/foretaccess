% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_cout.R
\name{surface_cout_construction}
\alias{surface_cout_construction}
\title{Build the construction-cost surface of a new forest road}
\usage{
surface_cout_construction(
  pre,
  config = foretaccess_config(),
  plan_eau = NULL,
  cours_eau = NULL,
  sol = NULL,
  interdit = NULL,
  surcout = NULL,
  methode_pente = c("bareme", "terrassement"),
  largeur_m = 4
)
}
\arguments{
\item{pre}{A \code{foretaccess_preprocessing} object (Lot 1): supplies the DEM grid,
terrain slope (\code{slope_pct}) and the complete-obstacle mask.}

\item{config}{A \code{foretaccess_config}; the cost scale lives in
\code{config$desserte$cout}.}

\item{plan_eau}{Optional water-body layer (bridges): \code{SpatRaster} (\verb{> 0} = water)
or \code{sf} polygons. Cells receive \code{cout_pont_m}.}

\item{cours_eau}{Optional stream layer (culverts): \code{SpatRaster} (metres of stream
per cell, or \verb{> 0}) or \code{sf} lines. Cells receive \code{cout_buse_m} scaled by the
crossed fraction.}

\item{sol}{Optional soil-class layer (\code{SpatRaster} of class codes or \code{sf}
polygons with a \code{classe} field); mapped through \code{config$desserte$cout$bareme_sol}.}

\item{interdit}{Optional forbidden-area layer (\code{SpatRaster} \verb{> 0} or \code{sf}
polygons): those cells are not crossable.}

\item{surcout}{Optional free additional surcharge (\code{SpatRaster}, €/m).}

\item{methode_pente}{How the slope term is priced: \code{"bareme"} (default, the
Lot 14 step function) or \code{"terrassement"} (spec 029, cut-and-fill volumes
priced per cubic metre -- continuous, and sensitive to platform width).
The default is deliberate: switching the slope term changes every route the
solver produces, so a side-by-side run on a real massif must come first.}

\item{largeur_m}{Target platform width (m), used by
\code{methode_pente = "terrassement"} only. Default 4.}
}
\value{
A \code{foretaccess_cout_construction} object: a list of two \code{SpatRaster}
aligned on the DEM — \code{cout} (€/m, \code{NA} outside the crossable zone) and
\code{franchissable} (logical).
}
\description{
Produces the per-cell \strong{construction cost} (monetary, €/m of road crossing the
cell) that the route solver (Lot 15) will propagate, plus a \strong{crossability}
layer (\code{NA}/\code{FALSE} = the solver must not traverse the cell). The cost is
additive: base cost + slope surcharge (by class) + soil surcharge + point
crossings (bridge over water bodies, culvert over streams) + a free
additional surcharge. Only the base cost is required; every optional layer that
is absent contributes nothing (never errors).
}
