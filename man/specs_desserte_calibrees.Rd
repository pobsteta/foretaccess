% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte-detectee.R
\name{specs_desserte_calibrees}
\alias{specs_desserte_calibrees}
\title{Calibration de référence de la détection (spec 026)}
\usage{
specs_desserte_calibrees()
}
\value{
Une liste : \code{geomorpho} (pour \code{dessertR::dsr_conductivite()}),
\code{surface} (pour \code{dessertR::dsr_sigma_surf()}) et \code{c_vessel} (pour
\code{dessertR::dsr_layers_dtm()}, une valeur par échelle).
}
\description{
Specs \strong{absolues} et \code{c_vessel} figés sur un jeu de référence, pour que le
\code{seuil} de \code{\link[=detecter_desserte]{detecter_desserte()}} désigne la même chose d'un site à l'autre.
}
\details{
\strong{Pourquoi figer.} \code{dessertR::dsr_calibrer_specs()} calibre sur les données
qu'on lui donne : appelé par AOI, il rendrait des specs justes localement mais
\strong{incomparables entre sites}, ce qui est précisément ce que le CA-26.5
interdit. On calibre donc \strong{une fois}, sur un jeu de référence, et on fige.

\strong{Valeurs produites par} \code{data-raw/calibrer_bornes_dsr.R} contre \strong{dessertR
1.1.0}, sur la dalle \code{LHD_FXX_0737_6385} (Chastel-Nouvel, 1 km², 32 tronçons,
7 561 m de desserte BD TOPO \code{piste} + \code{route}). Onze canaux retenus, AUC de
0,826 (\code{taux_penetration}) à 0,572 (\code{svf}).

\strong{Ordre de calibration, qui n'est pas indifférent} : \code{c_vessel} est mesuré
\strong{avant} la pile, et la pile construite avec — sinon les bornes seraient
calibrées sur une vesselness elle-même relative à l'emprise, et le défaut
réapparaîtrait un cran plus bas.

\strong{\code{densite_sousetage} est écarté}, bien que \code{dsr_calibrer_specs()} le
retienne (AUC 0,565). Motif : ses bornes sortent \strong{indéterminées}
(\verb{a = NA, b = NA}), les deux populations étant à zéro en médiane. Le garder le
ferait retomber sur la dérivation par quantiles, donc \strong{réintroduirait la
dépendance à l'emprise} sur ce canal — l'inverse du but. Ce n'est pas un
jugement sur sa valeur : dessertR 1.1.0 note qu'il mesure un \strong{état} et non
la présence d'une route, « une route recolonisée reste une route ».

\strong{Portée — un seul massif.} Lozère, 830–1 260 m, forêt de montagne. dessertR
1.1.0 calibre sur \strong{deux} massifs ; nous n'en avons qu'un, et il recouvre le
bloc \code{wsfi} à 54 \%. Ces valeurs sont \strong{provisoires} : elles ancrent, elles ne
généralisent pas. \code{specs = NULL} dans \code{\link[=detecter_desserte]{detecter_desserte()}} restaure les
défauts dessertR.
}
\seealso{
\code{\link[=detecter_desserte]{detecter_desserte()}}, \code{dessertR::dsr_calibrer_specs()},
\code{dessertR::dsr_c_vessel()}, \code{specs/026}.
}
