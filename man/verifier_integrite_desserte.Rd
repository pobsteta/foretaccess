% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte_integrite.R
\name{verifier_integrite_desserte}
\alias{verifier_integrite_desserte}
\title{Vérifie les contraintes d'intégrité du réseau de desserte (spec 025)}
\usage{
verifier_integrite_desserte(
  desserte,
  aoi = NULL,
  tol_noeud = 1,
  tol_test_topologie = 5,
  marge_bord_m = 10,
  largeur_dedupe = 0
)
}
\arguments{
\item{desserte}{Réseau de desserte : \code{sf} de lignes portant \code{classe}
(sortie d'\code{\link[=acquire_desserte]{acquire_desserte()}}).}

\item{aoi}{Emprise (\code{sf}/\code{sfc}) servant à détecter les composantes touchant le
bord. \code{NULL} : la cause \code{bord_aoi} n'est pas attribuée.}

\item{tol_noeud}{Distance (m) de collage des extrémités. Défaut 1.}

\item{tol_test_topologie}{Tolérance relâchée servant à tester la cause
\code{topologie} : une infraction qui disparaît à cette tolérance en relève.
Défaut 5.}

\item{marge_bord_m}{Distance (m) au bord de l'\code{aoi} en deçà de laquelle une
composante est réputée le toucher. Défaut 10.}

\item{largeur_dedupe}{Écart (m) de déduplication des parallèles passé à
\code{dsr_reseau()}. \strong{Défaut 0 : désactivée.} La valeur 3 de dessertR vise des
tracés forestiers corrigés indépendamment, où deux parallèles proches sont
un artefact. Sur un réseau incluant le \strong{réseau public} (chaussées
séparées, giratoires, contre-allées) elle coupe des liaisons \strong{réelles} :
mesuré sur l'AOI oracle à 1600 m de buffer, elle retirait 260 tronçons sur
1003 et fragmentait le graphe en 69 composantes dont 34 isolées, fabriquant
21 fausses infractions.}
}
\value{
Un objet \code{foretaccess_integrite} : \code{troncons} (\code{sf} avec \code{composant},
\code{connecte_public}, \code{viole_contrainte}, \code{cause}), \code{resume} (compteurs),
\code{courbe} (\code{NULL} ici, renseigné par \code{\link[=integrite_buffer_adaptatif]{integrite_buffer_adaptatif()}}),
\code{disponible} et \code{raison}.

\strong{Lire \code{disponible} avant \code{resume}.} Sans \code{dessertR}/\code{igraph}, ou si
\code{dsr_reseau()} n'aboutit pas, le diagnostic n'est pas effectué : \code{resume}
est alors tout en \code{NA} et un avertissement est émis. Un \code{n_infractions} à
\code{NA} signifie \strong{« on ne sait pas »}, jamais « aucune infraction » — ne
l'affichez pas comme un verdict. \code{\link[=dessertR_disponible]{dessertR_disponible()}} permet de poser la
question \emph{avant} de lancer le calcul.
}
\description{
Applique les deux contraintes de l'annexe ACCESSFOR (rapport février 2025,
p. 51) : une \strong{piste} doit être connectée à une route forestière ou au réseau
public, une \strong{route forestière} au réseau public. Les tronçons qui les violent
sont \strong{signalés, jamais retirés}.
}
\details{
\strong{Trois causes, de nature différente}, qu'il faut séparer avant toute
remédiation :
\itemize{
\item \code{bord_aoi} — la composante touche le bord de l'emprise : le raccordement est
probablement hors AOI, c'est un artefact de découpe et non un défaut ;
\item \code{topologie} — l'infraction disparaît quand on relâche la tolérance de
collage des nœuds : extrémités non nodées, écarts décimétriques ;
\item \code{reel} — ni l'un ni l'autre. C'est une information, pas une erreur.
}

\strong{Aucune donnée n'est modifiée.} Le recollage de nœuds (niveau 2) n'est
appliqué que si \code{tol_noeud} est explicitement augmenté ; la suppression de
composantes (niveau 3) \strong{n'existe pas} dans ce code. C'est ce qu'ACCESSFOR a
fait à la main, et l'automatiser retirerait de la desserte réelle dès que le
diagnostic se trompe. Sylvaccess lui-même se contente d'avertir.
}
\seealso{
\code{\link[=dessertR_disponible]{dessertR_disponible()}}, \code{\link[=integrite_buffer_adaptatif]{integrite_buffer_adaptatif()}},
\code{\link[=acquire_desserte]{acquire_desserte()}}, \code{specs/025}.
}
