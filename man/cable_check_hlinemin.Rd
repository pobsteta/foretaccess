% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cable_check_hlinemin}
\alias{cable_check_hlinemin}
\title{Faisabilité complète d'une travée : la charge balaie la longueur, on résout
les tensions à chaque position et on mesure la garde au sol. Renvoie la
garde minimale rencontrée (m), ou \code{-1} si la travée est infaisable (garde
hors \verb{[hline_min, hline_max]} ou tension au-delà de \code{tmax + 1000}). Sans
supports intermédiaires (Lot 4b).}
\usage{
cable_check_hlinemin(
  alts,
  h_alt,
  d,
  lo,
  fact,
  tho,
  tvo,
  xup,
  zup,
  f_o,
  tmax,
  hline_min,
  hline_max,
  q1,
  q2,
  q3,
  csize,
  eao
)
}
\arguments{
\item{alts}{Terrain altitudes under the line, sampled every 0.5 m (m).}

\item{h_alt}{Altitude difference between supports (m).}

\item{d}{Horizontal span between supports (m).}

\item{lo}{Unstretched cable length over the span (m).}

\item{fact}{Direction of the line (+1 or -1).}

\item{tho}{Horizontal tension with the load centred (N).}

\item{tvo}{Vertical tension with the load centred (N).}

\item{xup}{Horizontal position of the upper support (m).}

\item{zup}{Altitude of the upper support (m).}

\item{f_o}{Gravity force of the load and carriage (N).}

\item{tmax}{Admissible cable tension (N).}

\item{hline_min}{Minimum ground clearance of the cable (m).}

\item{hline_max}{Maximum height of the cable (m).}

\item{q1}{Linear mass of the skyline (kg/m).}

\item{q2}{Linear mass of the mainline / traction cable (kg/m).}

\item{q3}{Linear mass of the return cable (kg/m).}

\item{csize}{Sweep step of the load position (m).}

\item{eao}{Young's modulus times the cable section (N).}
}
\value{
The minimum ground clearance (m), or \code{-1} if infeasible.
}
\description{
Faisabilité complète d'une travée : la charge balaie la longueur, on résout
les tensions à chaque position et on mesure la garde au sol. Renvoie la
garde minimale rencontrée (m), ou \code{-1} si la travée est infaisable (garde
hors \verb{[hline_min, hline_max]} ou tension au-delà de \code{tmax + 1000}). Sans
supports intermédiaires (Lot 4b).
}
