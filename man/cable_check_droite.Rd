% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{cable_check_droite}
\alias{cable_check_droite}
\title{Pré-filtre géométrique : le câble est approximé par la corde entre supports
moins une flèche analytique. Renvoie 1 si le profil \verb{(line_x, line_z)} reste
dans les gardes entre les indices \code{pg+1} et \code{pd-1}, 0 sinon. Sans supports
intermédiaires (Lot 4b).}
\usage{
cable_check_droite(
  fact,
  h_alt,
  d,
  xup,
  zup,
  line_x,
  line_z,
  hline_min,
  hline_max,
  tmax,
  q1,
  q2,
  q3,
  f_o,
  pg,
  pd
)
}
\arguments{
\item{fact}{Direction of the line (+1 or -1).}

\item{h_alt}{Altitude difference between supports (m).}

\item{d}{Horizontal span between supports (m).}

\item{xup}{Horizontal position of the upper support (m).}

\item{zup}{Altitude of the upper support (m).}

\item{line_x}{Horizontal positions along the terrain profile (m).}

\item{line_z}{Altitudes of the terrain profile (m).}

\item{hline_min}{Minimum ground clearance of the cable (m).}

\item{hline_max}{Maximum height of the cable (m).}

\item{tmax}{Admissible cable tension (N).}

\item{q1}{Linear mass of the skyline (kg/m).}

\item{q2}{Linear mass of the mainline / traction cable (kg/m).}

\item{q3}{Linear mass of the return cable (kg/m).}

\item{f_o}{Gravity force of the load and carriage (N).}

\item{pg}{Index of the near support in the profile.}

\item{pd}{Index of the far support in the profile.}
}
\value{
1 if the span passes the pre-filter, 0 otherwise.
}
\description{
Pré-filtre géométrique : le câble est approximé par la corde entre supports
moins une flèche analytique. Renvoie 1 si le profil \verb{(line_x, line_z)} reste
dans les gardes entre les indices \code{pg+1} et \code{pd-1}, 0 sinon. Sans supports
intermédiaires (Lot 4b).
}
