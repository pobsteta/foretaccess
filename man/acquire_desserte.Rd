% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acquire-ign.R
\name{acquire_desserte}
\alias{acquire_desserte}
\title{Acquiert la desserte depuis BD TOPO (IGN WFS)}
\usage{
acquire_desserte(
  aoi,
  crs = 2154,
  cache_dir = tempdir(),
  overwrite = FALSE,
  country = "FR",
  classification = c("accessfor", "clsvac", "heuristique"),
  garder_hors_desserte = TRUE,
  politique_cache = "reacquerir"
)
}
\arguments{
\item{aoi}{Objet \code{sf}/\code{sfc} d'emprise, dans le CRS cible.}

\item{crs}{Code EPSG de sortie. Défaut 2154.}

\item{cache_dir}{Répertoire de cache.}

\item{overwrite}{Re-télécharger même si le cache existe. Défaut \code{FALSE}.}

\item{country}{Code pays ISO. Défaut \code{"FR"}.}

\item{classification}{Comment classer la BD TOPO en desserte Sylvaccess.
\code{"accessfor"} (défaut, spec 024) applique la table \strong{publiée} du rapport
ACCESSFOR (annexe p. 51), fondée sur \code{nature} \strong{seul} : « Route à 1 ou 2
chaussées » → \code{reseau_public}, « Route empierrée » et route forestière
nommée → \code{route}, « Chemin » → \code{piste}, \strong{tout le reste} (dont « Sentier »)
→ hors desserte, donc retiré. \code{"clsvac"} (spec 022) est le calage empirique
antérieur, qui utilisait \code{importance} ; \code{"heuristique"} l'historique deux
classes. Sur l'AOI oracle, \code{"accessfor"} et \code{"clsvac"} divergent sur 42 \%
des tronçons.}

\item{garder_hors_desserte}{Conserver les tronçons \code{hors_desserte}
(CL_SVAC = 0) dans la sortie ? \strong{Défaut \code{TRUE} depuis le 2026-07-30.}
Les retirer \strong{coupe le réseau} : mesuré sur l'AOI oracle, leur suppression
faisait passer les infractions de connectivité de 15 à 21 à 1600 m de
buffer. L'annexe ACCESSFOR le dit elle-même du rond-point — « non
nécessaire mais \strong{permet de garder un réseau intègre} ». Ils sont donc
conservés pour la \strong{topologie} et exclus de l'\strong{exploitation}.

Le consommateur qui en tire le bénéfice est
\code{\link[=verifier_integrite_desserte]{verifier_integrite_desserte()}} (spec 025), qui construit son graphe sur la
couche \code{sf} \strong{sans filtrer \code{classe}} : ces tronçons y soudent les
composantes. Ils ne franchissent en revanche aucune rasterisation —
\code{\link[=preprocess]{preprocess()}} les retire explicitement avant \code{terra::rasterize()}, de la
desserte comme des sources DFCI. Les sorties de \code{\link[=preprocess]{preprocess()}} sont donc
\strong{invariantes} sous ce paramètre ; il ne change que ce que voit le
diagnostic d'intégrité (et ce que l'on relit dans le GeoPackage).

\code{FALSE} reproduit la couche Sylvaccess stricte (classes 1/2/3 seulement).}

\item{politique_cache}{Que faire d'un cache produit avec \strong{d'autres
paramètres} ? \code{"reacquerir"} (défaut) refait l'acquisition, \code{"avertir"} sert
le cache en nommant ce qui diverge, \code{"echouer"} interrompt, \code{"ignorer"}
désactive le contrôle. Un cache \strong{sans provenance} (antérieur à la v1.29.0)
compte comme divergent. Cf. \code{\link[=cache_utilisable]{cache_utilisable()}} et \code{specs/027}.}
}
\value{
Un objet \code{sf} de lignes avec un champ \code{classe}.
}
\description{
Récupère \code{troncon_de_route}, reprojette, découpe sur l'AOI et dérive le champ
\code{classe} attendu par \code{\link[=preprocess]{preprocess()}}.
}
