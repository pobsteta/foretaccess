% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conduite.R
\name{conduire}
\alias{conduire}
\title{Balayage radial de conduite du porteur}
\usage{
conduire(pre, config, zone)
}
\arguments{
\item{pre}{Objet \code{foretaccess_preprocessing} (Lot 1).}

\item{config}{Objet \code{\link[=foretaccess_config]{foretaccess_config()}}.}

\item{zone}{\code{SpatRaster} logique des cellules candidates à la conduite (forêt roulable).}
}
\value{
Une liste de deux \code{SpatRaster} : \code{distance} (3D, m) et \code{allocation}.
}
\description{
Reproduit \code{fwd_azimuts_forest_roadnet} de Sylvaccess v3.6 : depuis chaque cellule du
réseau de desserte, un balayage \strong{360° au pas de 1°}, en ligne droite, jusqu'à
\code{distance_pente_forte_max_m}. Une cellule est conduisible si l'engin peut l'atteindre
sous trois contraintes de pente, distinctes de celles du skidder (spec 003 §4.2).
}
\details{
Contrairement au treuillage — dont ce balayage partage la géométrie (\code{\link[=.rayons]{.rayons()}}) —
les filtres portent sur la \strong{pente du terrain} à la cellule, en \strong{degrés}, et non sur
le gradient du rayon. À chaque cellule \code{j} d'un rayon d'azimut \code{az}, le rayon s'arrête
(\code{break}) au premier filtre violé :
\enumerate{
\item \strong{Pente en long, signée par l'altitude.} Le porteur ramène le bois vers la route,
chargé : si \code{alt_j > alt_route} le trajet est une descente
(\verb{pente_j ≤ pente_descente_max}), sinon une montée (\verb{pente_j ≤ pente_montee_max}).
\item \strong{Dévers}, dépendant de l'azimut :
\deqn{p_{lat,max} = \left| \theta_{lat} / \cos((90 - \Delta)\pi/180) \right|,
         \quad \Delta = (az - aspect_j) \bmod 180.}
Nul dans le sens de la pente (\verb{Δ → 0}, \verb{cos(90°) = 0}, seuil infini), maximal en
travers (\verb{Δ → 90}, seuil \verb{= θ_lat}). C'est le basculement latéral de la machine.
\item \strong{Distance cumulée en pente forte.} Un accumulateur croît de la longueur du pas là
où \code{pente_j > θ_lat} ; le rayon casse si le cumul dépasse \code{distance_pente_forte_max_m}.
}

La distance retenue est \strong{3D} : \verb{√(Hdist² + (alt_j − alt_route)²)}. Chaque cellule
garde la desserte la plus proche à ce sens.
}
\seealso{
\code{\link[=treuiller]{treuiller()}}, \code{\link[=.rayons]{.rayons()}}
}
