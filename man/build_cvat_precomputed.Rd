% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cvat_precompute.R
\name{build_cvat_precomputed}
\alias{build_cvat_precomputed}
\title{Precompute the CVAT relief over an AOI + buffer, guaranteeing coverage}
\usage{
build_cvat_precomputed(
  aoi,
  cache_dir,
  buffer_m = 100,
  res_lidar_m = 0.5,
  crs = 2154,
  mnt_existant = NULL,
  out = NULL,
  overwrite = FALSE,
  seuil_couverture = 0.9,
  country = "FR"
)
}
\arguments{
\item{aoi}{Working area: a path to a vector file, or an \code{sf}/\code{sfc} object.}

\item{cache_dir}{Directory for the (re-)acquired DEM and, by default, the CVAT.}

\item{buffer_m}{Buffer (m) grown around the AOI to define the extent to cover.
Default 100.}

\item{res_lidar_m}{LiDAR HD DEM resolution (m) used when (re-)acquiring.
Default 0.5 (native IGN LiDAR HD DTM).}

\item{crs}{Target CRS (EPSG code or WKT). Default 2154 (Lambert-93).}

\item{mnt_existant}{Optional path to an existing DEM (e.g. a native LiDAR
mosaic). Reused when it covers the AOI + buffer; otherwise ignored.}

\item{out}{Output path for the 8-bit CVAT. Default \code{cache_dir/cvat_8bit.tif}.}

\item{overwrite}{Force re-acquisition (if needed) and recomputation even if
\code{out} already exists.}

\item{seuil_couverture}{Minimal finite-cell fraction inside the extent for a
DEM to count as covering it. Default 0.9.}

\item{country}{Country code for \code{\link[=acquire_mnt]{acquire_mnt()}} layer resolution. Default
\code{"FR"}.}
}
\value{
Path to the 8-bit CVAT GeoTIFF (\verb{0..255}, \code{NA}->255), covering the
AOI + buffer.
}
\description{
Materializes the \strong{CVAT} (Combined VAT, \code{\link[=vat_combined]{vat_combined()}}) as an 8-bit GeoTIFF
covering the working extent (\strong{AOI widened by \code{buffer_m}}). If a DEM is passed
via \code{mnt_existant} and it \strong{covers the whole AOI + buffer}, it is used as is;
otherwise (missing LiDAR HD tiles -> the mosaic is too short, or mostly \code{NA})
the LiDAR HD DEM is \strong{re-acquired over the buffered extent} with
\code{\link[=acquire_mnt]{acquire_mnt()}} (WMS, which always spans the requested bbox) and the CVAT is
recomputed. This prevents a CVAT built on a partial mosaic from leaving holes
or a truncated relief in the comparator background.
}
\details{
foretaccess does \strong{not} download individual LiDAR HD tiles (\code{.copc.laz}): it
acquires the DEM through the IGN WMS, so re-acquisition inherently spans the
full AOI + buffer (no "missing tile" to chase). A caller that owns a native
0.5 m tile mosaic (e.g. an app's LiDAR ingestion) should pass it as
\code{mnt_existant}; it is kept whenever it already covers the extent, and only
replaced by the (coarser-looking) WMS DEM when it is too short.
}
\examples{
\donttest{
# Emprise + MNT deja couvrant -> pas de reacquisition reseau :
aoi <- sf::st_as_sf(sf::st_sfc(
  sf::st_polygon(list(rbind(c(0, 0), c(20, 0), c(20, 20), c(0, 20), c(0, 0)))),
  crs = 2154))
mnt <- terra::rast(xmin = -10, xmax = 30, ymin = -10, ymax = 30,
                   resolution = 1, crs = "EPSG:2154")
terra::values(mnt) <- 100 + terra::rowFromCell(mnt, seq_len(terra::ncell(mnt)))
d <- withr::local_tempdir()
terra::writeRaster(mnt, file.path(d, "mnt.tif"))
p <- build_cvat_precomputed(aoi, cache_dir = d, buffer_m = 5,
                            mnt_existant = file.path(d, "mnt.tif"))
terra::rast(p)
}
}
\seealso{
\code{\link[=vat_combined]{vat_combined()}}, \code{\link[=acquire_mnt]{acquire_mnt()}}.
}
