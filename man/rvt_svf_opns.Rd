% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rvt_svf_opns}
\alias{rvt_svf_opns}
\title{Sky-view factor and openness (RVT micro-relief channels, spec 021 J3).}
\usage{
rvt_svf_opns(
  height,
  nr,
  nc,
  resolution,
  radius_max,
  radius_min,
  num_directions,
  compute_svf,
  compute_opns
)
}
\arguments{
\item{height}{Elevation values (row-major, \code{NA}/\code{NaN} as NoData).}

\item{nr}{Number of raster rows.}

\item{nc}{Number of raster columns.}

\item{resolution}{Cell size (m); corrects the vertical horizon angle.}

\item{radius_max}{Maximal search radius in \strong{pixels}.}

\item{radius_min}{Minimal search radius in \strong{pixels} (noise reduction).}

\item{num_directions}{Number of azimuth directions swept.}

\item{compute_svf}{Whether to compute the sky-view factor.}

\item{compute_opns}{Whether to compute the openness (degrees).}
}
\value{
A list with \code{svf} (0..1) and \code{opns} (degrees), each row-major with
\code{NaN} where the input elevation is \code{NaN}; an unrequested channel is empty.
}
\description{
Ports RVT's \code{sky_view_factor_compute} (Relief Visualization Toolbox, Apache
2.0): a \code{roll}+\code{fmax} horizon sweep over \code{num_directions} azimuths and search
radii, from which the \strong{sky-view factor} (hemisphere) and \strong{openness}
(sphere, degrees) fall out of the same pass. These channels light up the
platform, embankments and shoulders of a forest track under canopy -- inputs
for road qualification (spec 021). No GIS in the crate (rule 3): R passes the
flat elevation grid and re-attaches the channels to the DEM. \strong{Negative
openness} is obtained by calling this on the negated DEM.
}
