% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte-detectee.R
\name{detecter_desserte_balayage}
\alias{detecter_desserte_balayage}
\title{Balayage de seuils pour la détection (spec 026, CA-26.5)}
\usage{
detecter_desserte_balayage(
  mnt,
  reference = NULL,
  las_source = NULL,
  seuils = seq(0.4, 0.8, by = 0.1),
  objets_connus = NULL,
  tol_recoupement = 10,
  ...
)
}
\arguments{
\item{mnt}{Modèle numérique de terrain (\code{SpatRaster}/chemin), \strong{1 m ou plus
fin} — le micro-relief d'une plateforme ancienne ne survit pas à 5 m.}

\item{reference}{Desserte connue à exclure (sortie d'\code{\link[=acquire_desserte]{acquire_desserte()}}).}

\item{las_source}{Nuage LiDAR pour le canal de surface. \code{NULL} : détection
sur la seule géomorphologie, « nettement moins sûre » selon dessertR.}

\item{seuils}{Seuils balayés. Défaut \code{seq(0.4, 0.8, by = 0.1)}, la plage
prescrite par la spec 026 §7.1.}

\item{objets_connus}{\code{sf} d'objets BD TOPO (cours d'eau, fossés, limites) dont
le recoupement vaut faux positif. \code{NULL} pour ne pas le mesurer.}

\item{tol_recoupement}{Demi-largeur (m) du corridor de recoupement. Défaut 10.}

\item{...}{Passé à \code{\link[=detecter_desserte]{detecter_desserte()}} (\code{buffer_ref}, \code{long_min},
\code{emprise}, \code{dtm_res}).}
}
\value{
Un \code{data.frame} : \code{seuil}, \code{n}, \code{km}, \code{km_recoupe}, \code{pct_recoupe}.
}
\description{
La spec 026 ne pose pas un seuil : elle \strong{mesure où la détection décroche}.
Ce balayage rend, pour chaque seuil, le linéaire détecté et la part recoupant
un objet \strong{BD TOPO connu} — des faux positifs quantifiés sans annotation.
}
\details{
Le recoupement automatique ne \strong{remplace pas} l'annotation sur orthophoto : il
la réduit. Un linéaire qui ne recoupe aucun objet connu n'est pas pour autant
une desserte — ce peut être une terrasse, une limite parcellaire non
cartographiée, ou une trace fossile. Le CA-26.5 n'est pas satisfait sans la
part annotée.
}
\section{Performance}{

\strong{\code{length(seuils)} fois le cout de \code{\link[=detecter_desserte]{detecter_desserte()}}} : la pile de
couches et le canal de surface sont rebatis a chaque seuil, rien n'est
memoise. Avec le defaut a cinq seuils, compter \strong{cinq fois} la mesure citee
par \code{\link[=detecter_desserte]{detecter_desserte()}}, pour le meme pic memoire. Outil de calage hors
ligne : ne jamais le cabler sur une action interactive.
}

\seealso{
\code{\link[=detecter_desserte]{detecter_desserte()}}, \code{specs/026}.
}
