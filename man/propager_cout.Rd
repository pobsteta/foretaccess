% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leastcost.R
\name{propager_cout}
\alias{propager_cout}
\title{Propagation de coût cumulé depuis des sources (service partagé)}
\usage{
propager_cout(surface_cout, sources, zone = NULL, cout_max = Inf)
}
\arguments{
\item{surface_cout}{\code{SpatRaster} de coût par cellule (\code{NA} = infranchissable).}

\item{sources}{Sources de la propagation : \code{SpatRaster} (toute cellule non
\code{NA} et non nulle est une source, sa valeur servant d'identifiant), ou objet
\code{sf} qui sera rasterisé sur la grille de \code{surface_cout}.}

\item{zone}{\code{SpatRaster} logique délimitant les cellules traversables, ou
\code{NULL} (défaut) pour n'exclure que les cellules de coût \code{NA}.}

\item{cout_max}{Coût cumulé maximal. Défaut \code{Inf} (aucun plafond).}
}
\value{
Un objet de classe \code{foretaccess_propagation} : une liste de trois
\code{SpatRaster} sur la grille de \code{surface_cout}.
\describe{
\item{\code{cout_cumule}}{coût cumulé depuis la source la moins coûteuse.}
\item{\code{allocation}}{identifiant de cette source (\code{Out_alloc} du \code{.pyx}).}
\item{\code{predecesseur}}{indice de la cellule précédente sur le chemin optimal,
qui permet de reconstruire le trajet avec \code{\link[=chemin_optimal]{chemin_optimal()}}.}
}
Les cellules non atteintes valent \code{NA} dans les trois couches.
}
\description{
Service de plus court chemin sur grille, \strong{sans aucune règle métier} : il est
mutualisé entre le skidder (Lot 2), le porteur (Lot 3) et le camion DFCI
(Lot 6). Reproduit \code{calcul_distance_de_cout()} de Sylvaccess v3.6.
}
\details{
Sémantique, fidèle au code source Sylvaccess (spec 002 §4.1) :
\itemize{
\item \strong{8-connexité} ; le pas vaut la résolution, ou \verb{résolution × sqrt(2)} en
diagonale ;
\item le coût d'un pas est \verb{surface_cout[cellule d'arrivée] × pas}. C'est le coût de
la \strong{cellule d'arrivée}, et non la moyenne des deux cellules — ce qui
distingue Sylvaccess de \code{\link[terra:costDist]{terra::costDist()}} et interdit de s'en servir tel quel ;
\item une cellule de coût \code{NA}, ou hors de \code{zone}, est \strong{infranchissable} ;
\item \code{cout_max} plafonne la propagation : au-delà, la sortie vaut \code{NA}.
}

L'algorithme est un Dijkstra à tas binaire. C'est le candidat naturel à un
portage Rust si la performance l'exige (ADR-001).
}
\examples{
cout <- terra::rast(nrows = 11, ncols = 11, xmin = 0, xmax = 11,
                    ymin = 0, ymax = 11, crs = "EPSG:2154")
terra::values(cout) <- 1
src <- terra::rast(cout)
src[6, 6] <- 1
prop <- propager_cout(cout, src)
prop$cout_cumule[6, 1]
}
\seealso{
\code{\link[=chemin_optimal]{chemin_optimal()}}
}
