% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{preprocess}
\alias{preprocess}
\title{Prétraitement commun aux moteurs d'accessibilité}
\usage{
preprocess(
  mnt,
  desserte,
  foret,
  obstacles_complets = NULL,
  obstacles_partiels = NULL,
  volume = NULL,
  parcellaire = NULL,
  config = foretaccess_config(),
  write_dir = NULL
)
}
\arguments{
\item{mnt}{MNT : chemin de raster ou \code{SpatRaster}. Définit la grille, la
résolution et le CRS de référence.}

\item{desserte}{Desserte : chemin de vecteur ou \code{sf} de lignes, avec un champ
\code{classe} dans \code{route}, \code{piste}, \code{dfci}.}

\item{foret}{Forêt : chemin de vecteur ou \code{sf} de polygones.}

\item{obstacles_complets}{Obstacles bloquant tous les engins (facultatif).}

\item{obstacles_partiels}{Obstacles spécifiques au skidder (facultatif ; la
sémantique est posée au Lot 2).}

\item{volume}{Volume sur pied : raster aligné sur la grille du MNT (facultatif).}

\item{parcellaire}{Parcellaire : vecteur de polygones (facultatif ; utilisé au
Lot 8 pour l'agrégation).}

\item{config}{Objet \code{\link[=foretaccess_config]{foretaccess_config()}}.}

\item{write_dir}{Répertoire où écrire les rasters en GeoTIFF/COG. \code{NULL}
(défaut) : tout reste en mémoire.}
}
\value{
Un objet de classe \code{foretaccess_preprocessing} : une liste dont les
rasters partagent exactement la grille du MNT.
\describe{
\item{\code{slope_pct}}{pente en pourcentage.}
\item{\code{aspect_deg}}{exposition en degrés depuis le nord (plat = \code{NA}).}
\item{\code{foret_mask}}{1 dans les polygones forêt, 0 ailleurs.}
\item{\code{desserte}}{raster catégoriel de classe de desserte (\code{NA} hors desserte).}
\item{\code{desserte_sf}}{la desserte vectorielle, conservée pour le least-cost (Lot 2).}
\item{\code{obstacles_complets_mask}, \code{obstacles_partiels_mask}}{1 où obstacle, 0 sinon.}
\item{\code{exclusion_mask}}{1 là où la pente dépasse le seuil d'abattage manuel.}
\item{\code{volume}}{raster de volume aligné, ou \code{NULL}.}
\item{\code{parcellaire}}{objet \code{sf}, ou \code{NULL}.}
\item{\code{grid}}{métadonnées de grille (emprise, résolution, dimensions, CRS).}
\item{\code{config}}{la configuration utilisée.}
\item{\code{fichiers}}{chemins écrits si \code{write_dir}, sinon \code{NULL}.}
}
}
\description{
Socle commun aux quatre moteurs (skidder, porteur, câble, DFCI). Transforme des
entrées SIG hétérogènes en un jeu de rasters \strong{alignés sur la grille du MNT},
après validation stricte. Aucune règle de moteur n'est appliquée ici.
}
\details{
Chaque entrée est acceptée soit comme \strong{chemin de fichier}, soit comme objet
déjà chargé (\code{SpatRaster} pour les rasters, \code{sf} pour les vecteurs), cf. ADR-004.
Les seuils proviennent de \code{config} — aucune valeur métier n'est codée en dur
(ADR-003).
}
\section{Bordures}{

\code{slope_pct}, \code{aspect_deg} et donc \code{exclusion_mask} valent \code{NA} sur la première
couronne de cellules : le calcul de pente exige les 8 voisins.
}

\examples{
toy <- system.file("extdata/toy", package = "foretaccess")
pre <- preprocess(
  mnt = file.path(toy, "mnt.tif"),
  desserte = file.path(toy, "desserte.gpkg"),
  foret = file.path(toy, "foret.gpkg")
)
pre$grid$res
}
