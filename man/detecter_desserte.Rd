% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desserte-detectee.R
\name{detecter_desserte}
\alias{detecter_desserte}
\title{Détecte la desserte absente de la référence, sur le MNT (spec 026)}
\usage{
detecter_desserte(
  mnt,
  reference = NULL,
  las_source = NULL,
  seuil = 0.6,
  buffer_ref = 15,
  long_min = 30,
  emprise = NULL,
  dtm_res = 1,
  methode = "squelette",
  specs = specs_desserte_calibrees(),
  poids = NULL,
  seuil_vessel = NULL
)
}
\arguments{
\item{mnt}{Modèle numérique de terrain (\code{SpatRaster}/chemin), \strong{1 m ou plus
fin} — le micro-relief d'une plateforme ancienne ne survit pas à 5 m.}

\item{reference}{Desserte connue à exclure (sortie d'\code{\link[=acquire_desserte]{acquire_desserte()}}).}

\item{las_source}{Nuage LiDAR pour le canal de surface. \code{NULL} : détection
sur la seule géomorphologie, « nettement moins sûre » selon dessertR.}

\item{seuil}{Seuil de binarisation de \code{p_desserte}. Défaut 0,6 (dessertR).
La spec 026 prescrit un \strong{balayage 0,4 → 0,8} pour la validation, pas un
seuil unique — voir \code{\link[=detecter_desserte_balayage]{detecter_desserte_balayage()}}.}

\item{buffer_ref}{Demi-largeur (m) du corridor de référence exclu. Défaut 15.}

\item{long_min}{Longueur minimale (m) d'un linéaire retenu. Défaut 30.}

\item{emprise}{Emprise polygonale restreignant le balayage (régime
\code{corridor}) ; \code{NULL} balaye toute la grille (régime \code{complet}). La spec 026
prescrit \code{complet} pour la \strong{validation} — un corridor biaise le
dénominateur du taux de faux positifs vers les zones déjà intéressantes — et
\code{corridor} en production.}

\item{dtm_res}{Résolution (m) de la grille de référence. Défaut 1.}

\item{methode}{Vectoriseur, passé tel quel à \code{dessertR::dsr_detecter()}.
\strong{Défaut \code{"squelette"}, nommé et non subi} : depuis dessertR 1.1.0,
\code{"auto"} résout vers \code{"agent"}, mais l'agent ne peut pas s'amorcer quand
\code{buffer_ref > 0} — \code{dsr_amorces()} filtre ses amorces sur \code{!is.na(p)} à
l'extrémité des tronçons de référence, qui est précisément la zone que
\code{dsr_indice_detection()} vient de masquer. \code{"auto"} replierait donc sur le
squelette \strong{en silence}, et la chaîne mesurée changerait sans préavis au
jour où l'amont corrigera. Voir \code{specs/026} §6.0.1 (précondition P5).}

\item{specs}{Bornes d'appartenance. \strong{Quatre formes acceptées} :
\itemize{
\item \code{\link[=specs_desserte_calibrees]{specs_desserte_calibrees()}} (défaut) — bornes figées, imbriquées
(\code{geomorpho}/\code{surface}/\code{c_vessel}) ;
\item \code{"auto"} — \strong{calibre sur place} avec \code{dessertR::dsr_calibrer_specs()},
à partir du MNT et de la \code{reference} fournis. C'est la réponse au conseil
de dessertR quand les bornes figées saturent (« des bornes calibrées sur
un AUTRE massif ne se transportent pas ») : l'appelant suit ce conseil
sans avoir à connaître deux vocabulaires de specs. \strong{Exige \code{reference}.}
\code{surface} et \code{c_vessel} restent figés, une calibration ne les produisant
pas ;
\item la sortie \strong{plate} de \code{dsr_calibrer_specs()$specs} — reconnue à sa forme
et promue en \code{geomorpho}, cf. \code{\link[=specs_depuis_calibration]{specs_depuis_calibration()}} ;
\item \code{NULL} — \strong{restaure les specs de dessertR}, dont les bornes sont dérivées par
quantiles de l'emprise — le \code{seuil} cesse alors d'être comparable d'un site
à l'autre.
}}

\item{poids}{Poids des trois termes de la fusion, vecteur nommé
\code{c(geo=, surf=, vessel=)}. \code{NULL} (défaut) laisse ceux de \code{dsr_detecter()}.

\strong{À connaître avant de régler quoi que ce soit} : \code{dsr_detecter()} fusionne
en \strong{moyenne géométrique pondérée}, donc dominée par son plus \strong{petit}
terme — un poids n'y dose pas une contribution, il arme un \strong{veto}. Mesuré
sur le bloc \code{wsfi} (campagne CA-26.5) : le terme \code{vessel}, à son poids par
défaut de 1, ramène \code{p_desserte} de 0,210 à \strong{0,001} sur des pistes
réelles, parce que \code{vesselness} est un détecteur de crêtes creux dont
1,62 \% des cellules atteignent la rampe. \code{c(geo = 1, surf = 0.5, vessel = 0)}
neutralise ce veto.}

\item{seuil_vessel}{Début de la rampe d'appartenance sur \code{vesselness}. \code{NULL}
(défaut) laisse celui de \code{dsr_detecter()} (0,3) — à comparer aux bornes
calibrées du même canal dans \code{specs$geomorpho}, qui saturent à 0,07.}
}
\value{
Un \code{sf} de \code{LINESTRING} avec \code{source = "detectee"} et \code{p_desserte}.
Sans \code{dessertR}, une couche vide et un message — jamais d'échec. L'attribut
\strong{\code{canal_surface}} (logique) dit si le canal de surface a réellement été
calculé : il est présent sur \strong{toute} sortie, vide comprise, pour qu'un
résultat nul se lise sans ambiguïté.
}
\description{
Cherche dans le \strong{micro-relief} les linéaires que la BD TOPO ne porte pas :
pistes anciennes, cloisonnements, tracés effacés du couvert. Rend une couche
\strong{candidate}, jamais une desserte utilisable.
}
\details{
\strong{La sortie n'est pas de la desserte.} On détecte des linéaires creux, et le
micro-relief garde aussi les drains, fossés, limites parcellaires et
terrasses. Un tronçon détecté n'a ni largeur, ni état, ni portance mesurés :
il doit passer \code{\link[=qualifier_desserte]{qualifier_desserte()}} avant d'entrer dans une conception de
réseau. Aucun chemin ne doit permettre à un candidat d'entrer dans
\code{desserte_existante} sans qualification.

\strong{Deux gisements disjoints.} Cette détection trouve ce qui est \emph{effacé du
couvert mais lisible au sol} ; \code{\link[=acquire_desserte_osm]{acquire_desserte_osm()}} trouve ce que
\emph{quelqu'un a cartographié}. Les deux alimentent la même couche candidate, avec
\code{source} distinct.

\strong{Éloigne d'ACCESSFOR délibérément} : ACCESSFOR consomme la BD TOPO seule.
Ne jamais activer dans une comparaison ACCESSFOR.
}
\section{Performance}{

\strong{La fonction la plus couteuse du paquet.} Mesure nemetonshiny du
2026-08-12 : \strong{729 s et plus de 8 Go de pic} sur 1 855 ha, MNT LiDAR 0,5 m.

Deux postes, qui ne se compensent pas :
\itemize{
\item la \strong{pile de couches} (\code{dsr_layers_dtm()}) tient toute l'emprise en memoire
a la resolution du MNT -- d'ou le pic, proportionnel a la surface DIVISEE par
le carre de la resolution ;
\item le \strong{canal de surface} relit le nuage LiDAR dalle par dalle : compter la
taille du nuage en plus.
}

Ne jamais cabler cette fonction sur un bouton synchrone ; prevoir un worker
separe. Et \strong{borner l'emprise, pas la resolution} : passer de 0,5 m a 5 m ne
fait pas gagner du temps, cela fait perdre le signal -- 0 canal retenu sur 7 a
5 m contre 5 sur 7 a 0,5 m (mesure nemetonshiny). Sur un poste de 31 Go
partage, une emprise de l'ordre de 2 000 ha est deja le plafond raisonnable.
}

\section{Place dans le flux}{

\code{\link[=detecter_desserte]{detecter_desserte()}} et \code{\link[=qualifier_desserte]{qualifier_desserte()}} sont \strong{sequentielles, pas
exclusives} : la premiere trouve l'ABSENT (des linéaires candidats hors du
réseau connu), la seconde requalifie l'EXISTANT (largeur, état, portance
mesurés au LiDAR). L'enchaînement naturel est
\code{acquire_desserte()} -> \code{detecter_desserte()} -> fusion -> \code{qualifier_desserte()},
car la sortie de détection est \strong{candidate} : sans largeur ni portance, elle
n'est pas consommable par \code{\link[=preprocess]{preprocess()}}. Les enchaîner dans l'autre sens
qualifierait un réseau auquel il manque encore ce qu'on cherche.
}

\seealso{
\code{\link[=detecter_desserte_balayage]{detecter_desserte_balayage()}}, \code{\link[=qualifier_desserte]{qualifier_desserte()}},
\code{\link[=acquire_desserte_osm]{acquire_desserte_osm()}}, \code{specs/026}.
}
