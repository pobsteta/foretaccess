% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acquire-obstacles-ign.R
\name{acquire_obstacles_bdtopo}
\alias{acquire_obstacles_bdtopo}
\title{Acquire ACCESSFOR-conformant obstacles from BD TOPO and INPN (spec 022 volet B)}
\usage{
acquire_obstacles_bdtopo(
  aoi,
  crs = 2154,
  cache_dir = tempdir(),
  overwrite = FALSE,
  country = "FR",
  routes_importance_max = 3L,
  tampon_m = 5,
  zonages = TRUE
)
}
\arguments{
\item{aoi}{Objet \code{sf}/\code{sfc} d'emprise, dans le CRS cible.}

\item{crs}{Code EPSG de sortie. Défaut 2154.}

\item{cache_dir}{Répertoire de cache.}

\item{overwrite}{Re-télécharger même si le cache existe. Défaut \code{FALSE}.}

\item{country}{Code pays ISO. Défaut \code{"FR"}.}

\item{routes_importance_max}{Main roads with BD TOPO \code{importance} at most this
are added as obstacles (autoroutes/nationales/départementales structurantes).
Default 3. \code{NA} disables road obstacles.}

\item{tampon_m}{Buffer (m) applied to line obstacles. Default 5.}

\item{zonages}{Include the INPN/Patrinat regulatory exclusions? Default \code{TRUE}.}
}
\value{
An \code{sf} of \code{MULTIPOLYGON} obstacles in \code{crs}, clipped to \code{aoi}. Empty
layers are skipped; if nothing is found the result has zero rows.
}
\description{
Assembles the \strong{obstacle layer ACCESSFOR uses} (report Feb. 2025, sec. 2.3.4)
into a single \code{sf} of polygons ready for \code{preprocess(obstacles_complets = )}:
BD TOPO obstacles (watercourses, water surfaces, railways, buildings, main
roads) \strong{plus} the INPN/MNHN regulatory exclusions (biotope-protection orders,
national and regional nature reserves, biological reserves, and the \strong{integral
reserve} of national parks -- not the whole park). Distinct from
\code{\link[=acquire_obstacles]{acquire_obstacles()}} (OpenStreetMap source).
}
\details{
Line features (watercourses, railways, main roads) are buffered by half a
\code{tampon_m} so they rasterise to a continuous barrier; polygons are kept as is.
National parks are filtered on their \code{zone} attribute to the \strong{integral
reserve} only -- excluding the whole park would over-block massively. \code{PNR},
\code{ZNIEFF}, Natura 2000 and hunting reserves are \strong{not} ACCESSFOR exclusions and
are never included. On a massif without reserves the effect comes from the BD
TOPO obstacles alone.
}
\seealso{
\code{\link[=acquire_obstacles]{acquire_obstacles()}} (OSM), \code{\link[=preprocess]{preprocess()}} (consumes
\code{obstacles_complets}), \code{specs/022}.
}
