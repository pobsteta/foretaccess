% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treuillage.R
\name{treuiller}
\alias{treuiller}
\title{Distance de treuillage depuis la desserte (balayage radial)}
\usage{
treuiller(mnt, desserte, zone, config = foretaccess_config())
}
\arguments{
\item{mnt}{\code{SpatRaster} du MNT.}

\item{desserte}{\code{SpatRaster} des cellules de desserte (non \code{NA} = desserte).}

\item{zone}{\code{SpatRaster} logique des cellules treuillables.}

\item{config}{Objet \code{\link[=foretaccess_config]{foretaccess_config()}}.}
}
\value{
Une liste de deux \code{SpatRaster} : \code{distance} (m, distance 3D ; \code{NA} si
non treuillable) et \code{allocation} (indice de la cellule de desserte).
}
\description{
Reproduit \code{skid_debusq_RF()} de Sylvaccess (\code{sylvaccess_cython3.pyx:3116}).
Le treuillage n'est \strong{pas} un plus court chemin : depuis chaque cellule de
desserte, on balaie 360 azimuts au pas de 1°, en \strong{ligne droite}.
}
\details{
Le long de chaque rayon, un pixel est treuillable si :
\itemize{
\item la \strong{distance 3D} \code{sqrt(Hdist^2 + dz^2)} ne dépasse pas
\code{\link[=distance_treuillage_max]{distance_treuillage_max()}} pour la pente signée du rayon (test appliqué
seulement au-delà de \code{min(amont, aval)}) ;
\item la \strong{corde du câble}, tendue depuis la desserte à
\code{hauteur_attache_treuil_m}, reste au-dessus du sol et sous
\code{hauteur_degagement_max_m} \strong{en tout point intermédiaire} ;
\item le pixel est dans la zone treuillable.
}

Dès qu'une de ces conditions échoue, le rayon est \strong{interrompu} : rien n'est
atteignable au-delà par cet azimut. Chaque pixel retient la plus petite
distance, tous azimuts et toutes cellules de desserte confondus.

Écart assumé avec la source : dans le \code{.pyx}, le test de dégagement n'est
évalué que si le pixel serait amélioré, ce qui rend l'interruption du rayon
dépendante de l'ordre de parcours des cellules de desserte. Ici il est
toujours évalué — c'est la sémantique visée, et elle est déterministe.
}
