% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessfor.R
\name{comparer_accessfor}
\alias{comparer_accessfor}
\title{Compare debarking classes against the IGN ACCESSFOR layer}
\usage{
comparer_accessfor(
  cl,
  accessfor,
  champ = "class",
  config = foretaccess_config()
)
}
\arguments{
\item{cl}{A categorical \code{classes_debardage()} raster (values \verb{1..k+3}).}

\item{accessfor}{An \code{sf}/\code{SpatVector} of ACCESSFOR polygons carrying an integer
class column. Must share the CRS of \code{cl} (no implicit reprojection, ADR-004).}

\item{champ}{Name of the ACCESSFOR class column. Default \code{"class"}.}

\item{config}{The \code{foretaccess_config} whose bands define the crosswalk.
Default \code{\link[=foretaccess_config]{foretaccess_config()}}.}
}
\value{
An object of class \code{foretaccess_accessfor_compare}: \code{matrice} (area in
ha, our class in rows x ACCESSFOR class in cols, on the common mask),
\code{accord_global} (share of area on the diagonal), \code{accord_agrege}
(accessible/non-accessible 2x2 agreement), \code{surface_ha} (common, and each
mask-only area excluded from the comparison), and \code{config}.
}
\description{
Confronts a \code{\link[=classes_debardage]{classes_debardage()}} raster with the IGN \strong{ACCESSFOR} reference
(skidder or porteur), on the grid of the raster itself. ACCESSFOR is
rasterised in \strong{nearest-neighbour} (a class code must never be interpolated),
mapped onto our value scheme via \code{\link[=accessfor_correspondance]{accessfor_correspondance()}}, and the two are
cross-tabulated \strong{only on the intersection of the forest masks} -- the three
masks (ours, ACCESSFOR, ACCESSFOR-V3) differ, so any global figure that
ignored this would be dominated by a masking artefact.
}
\section{Lecture}{

The robust number is the \strong{aggregated} agreement (accessible vs not): a strong
disagreement on the far bands is expected (machine parameters, reference road
network), while an accessible-vs-inaccessible flip is a signal to investigate.
The \code{inexploitable} class only appears if \code{classes_debardage()} was given \code{pre};
its ACCESSFOR threshold (slope) is not published, so a disagreement there is a
parameter artefact, not a defect.
}

\seealso{
\code{\link[=accessfor_correspondance]{accessfor_correspondance()}}, \code{\link[=classes_debardage]{classes_debardage()}}.
}
